
Implement the requested capability end to end. Define the behavioral contract,
update every production entry point that owns it, add focused regression tests,
and run the narrow and repository-standard verification commands. Keep scope
bounded, but do not substitute a smaller outcome for the requested one.
