
Map the repository around the requested concern. Identify public entry points,
runtime assembly, data ownership, control flow, persistence boundaries, tests,
and relevant documentation. Explain how the pieces connect and which files are
likely to change, but do not propose a broad rewrite without evidence.
