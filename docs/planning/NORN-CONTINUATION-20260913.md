# Norn continuation — 13 September 2026, Melbourne

Tom authorized completing the remaining list, with regular disk checks, build cleanup, AST-grep and strict Clippy. The JSON companion records the queue; September 8 planning and all 54 original programme rows are preserved beside it. Historical statuses are not fresh verification. This product queue links to the estate index rather than replacing it.

At this continuation's start: branch `codex/norn-integration-candidate`, commit `cc3fd60`, installed preview.11. The latest installed checkpoint is recorded below and in the JSON companion. Existing voice/driven work must be audited before classifying it as absent. Local installation is distinct from source-bound venue verification and main landing.

## Order

- **D01** Compaction failure diagnosis, continuity policy, and terminal-safe diagnostics. in_progress.
- **D02** Resumed conversation history and earlier-page loading. in_progress.
- **D03** Correct resume selection, action-log times, and child-history access. in_progress.
- **D04** Tool lifecycle presentation and readable input/output. queued_for_live_audit.
- **D05** Typed channel history and faint inbound-content styling. queued_for_live_audit.
- **D06** Diff highlighting, follow-latest, and remaining reading/composer polish. queued_for_live_audit.
- **D07** In-session action-log search and compaction retrieval links. queued_for_live_audit.
- **D08** First external voice adapter: read-aloud. queued_for_live_audit.
- **D09** Dictation into the Iridium draft. queued_for_live_audit.
- **D10** Seat identity and lifetime-memory design decisions. queued_for_live_audit.
- **D11** Derived lifetime timeline, temporal queries, and retention protections. queued_for_live_audit.
- **D12** Authored memories and addenda anchored to events and time. queued_for_live_audit.
- **D13** Agent directory, selected conversations, direct @ addressing and CC. queued_for_live_audit.
- **D14** Visible queued messages. queued_for_live_audit.
- **D15** Independent live session host and attach/detach. queued_for_live_audit.
- **D16** Linked goals/plans/tasks and additive feature activations. queued_for_live_audit.
- **D17** Editable workspace, session controls, request-time output contracts. queued_for_live_audit.
- **D18** Cross-harness conversation import and continuation. queued_for_live_audit.
- **D19** Liminal extension interfaces and Manifold reconciliation. queued_for_live_audit.
- **D20** Release, installer, remaining review findings and cleanup. queued_for_live_audit.

S01 (session-storage duplication) starts with D01 diagnosis. No live session is edited, deleted or migrated during investigation. U01 (per-file version browsing) extends D06/D17. Later rows with unresolved design decisions must name those decisions while independent work continues; nothing is silently dropped.

## First executable slice: D01.1 — repeated compaction input

Problem: `loop/compaction.rs` passes the entire raw event prefix to summarization. `session/conversion.rs` explicitly requires an already-filtered prompt view. Previous summaries and their superseded originals are therefore both sent again. In the reported session, 30 persisted compaction audits contain two semantic summaries, seven context-window failures and 21 HTTP-400 failures (response bodies were omitted, so their precise causes remain unknown). The file is 57,367,754 bytes at inspection; its latest compaction row is 906,327 bytes. These are snapshot measurements, not a frozen-store claim. No transcript content was exported.

Wall: `crates/norn/src/loop/compaction.rs`, `crates/norn/src/loop/compaction_prompt.rs`, `crates/norn/src/loop/compaction_prompt_tests.rs`, `crates/norn/src/loop/mod.rs`, this JSON/Markdown queue, September 8 planning copies, and release notes. If repair requires changing persistence schemas, summary failure policy, provider contracts or diagnostic routing, create the next separately scoped slice first.

Design provenance: NEXT-WORK-20260908 D01; `loop/context.rs` authoritative prompt visibility and local-tool atomicity; `session/conversion.rs` prompt-only projection contract; NUI-005 continuity follow-ups. Retain current summary, visible older messages and their tool pairs; exclude suppressed/superseded originals and retained recent turns. Preserve cancellation, durable event identities and current fallback policy in this slice. This fixes duplicated summary input, not all potential HTTP-400 causes or stored duplication.

Acceptance: a regression must fail on the original raw-prefix implementation, then pass for repeated compaction and persisted-mark restoration; verify prior summary exactly once, old originals absent, retained turns absent, and original store untouched by selection. Include tool-pair projection and provider-request assertions. Run existing compaction/summarization/replay tests, targeted AST scans and strict Clippy. Greps: `rg 'compaction_prompt|summary_prompt_events' crates/norn/src/loop`; `rg 'repeated_compaction|restored_marks' crates/norn/src/loop/compaction_prompt_tests.rs`.

Resource baseline: 28 GiB available, 2.8 GiB current target, 176 MiB release receipts/rollbacks. Target stays `var/build-preview10` under the repository; its name identifies the existing cache, not the next version.

## D02.1 — load earlier history at the scroll boundary

Wall: `crates/norn-tui/src/app/render/navigation.rs`, `crates/norn-tui/src/app/render/navigation_tests.rs`, this queue and release notes. Provenance: NEXT-WORK D02 and NUI-005 retained history; existing `Transcript::load_older` owns asynchronous bounded page reads. A backward scroll that exhausts loaded rows while `has_older` is true requests one earlier page through the existing loader. No full-history startup load, no I/O inside paint, no competing history owner. Forward scrolling and the true beginning of history request nothing. Accepted pages retain the existing anchor and draft. The remainder of a large gesture is not replayed automatically in this slice; another scroll moves into the newly loaded page. Smooth residual motion and boundary status remain follow-ups.

Acceptance: real bound EventStore tail/older pages, actual navigation and load_visible path, coalesced in-flight history requests, no duplicate records, pinned viewport and draft preserved; forward/beginning no-load regressions. Run retained navigation/history/TUI tests, AST scans and strict Clippy.

Installation wall extension for D01.1/D02.1: Cargo.toml, Cargo.lock and README.md may record the next local preview version once these fixes pass. Installation receipts and rollback stay under repository var/releases; this does not extend either code wall.

## Verification checkpoint

D01.1: all three new provider-request regressions failed with the original raw-prefix selection, then passed after selection uses the existing prompt visibility and atomic tool projection. The full core library suite passed: 4,709 tests, zero failed/ignored. Logs: `var/verification/continuation-20260913/d01-raw-prefix-regression.log` and `d01-core-tests.log`. No live-provider request was made by these regression fixtures.

AST scans on the new modules and changed navigation files found no unwrap/expect calls, lint bypass attributes or discarded-result bindings. `loop/compaction.rs` retains two pre-existing test allowance attributes; these are recorded policy debt, not claimed clean. The installed version remains preview.11 until the preview.12 binary and TUI verification complete.

S01 observation: one live session snapshot had 11,455,311 serialized bytes in canonical assistant response items, plus 7,335,349 bytes in populated compatibility reasoning fields and 2,739,352 bytes in compatibility tool-call fields. Field totals alone do not establish which bytes can safely be removed. Older readers, replay projections and publication commitments must be checked before changing serialization. Compaction planning and commit validation also currently clone the raw event store; that allocation audit is separate from D01.1's reduced summary input.

D02.1 verification: all 939 TUI library tests passed with the installed CLI's libyggd-ast feature set, including the actual navigation-to-history-loader fixture. Strict release-profile workspace/all-target Clippy with live-api-smoke enabled passed, as did format and diff checks. Live provider smoke was compiled/linted only. Release build/install remain pending at this checkpoint. Obsolete preview.11 core/TUI test executables were removed only after checks finished; current executables, dependency cache, logs and rollback binaries remain.

## Installed checkpoint — 13 September 2026, 03:27 Melbourne

Preview.12 installed from `0d87f8a4b99af4982dc09868bccb16b8d4680847`; SHA-256 `ded53f31ea3829cfe530d8a70d4efb6cd19b85ae48e62a6cb5dad3cea20fbc0d`. `var/releases/preview.12-continuity/installation.json` records the atomic replacement and preview.11 rollback. Release build and actual-executable Kitty keyboard/restoration probe passed after the 4,709 core / 939 TUI tests, strict release workspace/all-target Clippy and formatting checks. Mac announcement completed. No live-provider or physical Herdr pass is claimed.

D01.1 and D02.1 are installed local slices; the overall D01 and D02 deliveries remain in progress. Next: compaction failure policy and semantic projection/diagnostic handling, with a separate file wall; S01 storage compatibility/publication audit continues. Tool lifecycle styling, channel distinction and file-pane highlighting/version browsing remain on D04–D06/D17, not included here.

Disk checkpoint: 25 GiB available; one 3.5 GiB repository-local build cache and 235 MiB release receipts/rollbacks. Removed 191,361,440 bytes of identified obsolete test executables during this goal. Current artifacts and all rollback binaries remain.

The installed Aion CLI confirmed an active `repo_battery_205` route (`56cd8d0a1d3ba94811c605432b0e3730a4f87d1ab9cfe732c62cda46f0f5b0e9`). This was a read-only availability check; no battery has been dispatched and main has not advanced. Inspect its current input and venue resource contract before dispatching.

## D01.2 — semantic summary failure preserves context

Tom's authorized continuity repair now replaces the existing automatic mechanical fallback policy: a provider failure or unusable summary stops the step with a typed error, leaves all context marks and the compaction trigger unchanged, and does not send the oversized ordinary request. Cancellation remains cancellation. Explicit manual mechanical compaction is unchanged. The error retains the provider cause or stop reason/text length and the known summary usage; preflight records a versioned `loop.compaction_failed` audit with that usage before returning the error. No failure is relabelled as successful compaction. Retry classification follows the underlying provider cause; unusable output is terminal.

Wall: `crates/norn/src/error.rs`, `error/{subsystems.rs,compaction.rs}`, `loop/{compaction.rs,compaction_failure.rs,inflight_compaction.rs,mod.rs}`, `loop/runner/prompt.rs`, `loop/runner/tests/local_compaction.rs`, `loop/compaction_failure_tests.rs`, Cargo.toml, Cargo.lock, README.md, release notes and this queue. Provenance: NEXT-WORK D01's explicit failure-policy recommendation, no-silent-fallback house rule, and existing cancellation/usage contracts. A later slice handles general stderr tracing ownership and semantic rendering of opaque response items.

Acceptance: permanent provider failure, empty/truncated output and cancellation commit no compaction or hidden-event marks; trigger remains re-usable; actual runner sends no normal model request after failure; original accepted input survives; known rejected-summary usage and typed error survive in the returned error and durable audit; a later successful retry can compact. Run core/runner and TUI regressions, strict Clippy, formatting and changed-file AST scans. Historical fallback records remain readable. No live session rewrite or inferred recovery of facts lost by earlier semantic fallbacks is claimed.

D01.2 also permits source-documentation correction in `crates/norn/src/loop/summarization.rs`; the summary renderer is unchanged by this slice.

## D01.2 local verification checkpoint

Source fix `6407b1765ab846d6a71c9c4273e9a037fc1a9e6c`: all 4,711 core and 939 TUI library tests passed, zero failed or ignored. Strict release workspace/all-target Clippy with live-api-smoke enabled passed (compiled/linted only, no live provider request). Format and whitespace checks passed. New compaction modules have no unwrap/expect or bypass/discard matches; scans of all 11 changed Rust files record existing counts separately with no increase. Proof logs are under `var/verification/continuation-20260913/d01-failure-*`. An initial test fixture lacked completed assistant turns and was corrected; the final suite includes the real runner error-path regression.

The next slice is D01.3's summary-only projection of opaque encrypted provider items, then terminal diagnostic ownership and S01's storage/allocation audit. This change preserves failed context; it does not yet prevent every oversized summary request.

Venue input was recovered from the first event of completed workflow `62bd3f87-a13b-4fa7-bce5-6dc07fec1229`, under the still-active package `56cd8d0a1d3ba94811c605432b0e3730a4f87d1ab9cfe732c62cda46f0f5b0e9`: `subject_repo`, exact `subject_ref`, `workspace_root=/home/aion/venue/.battery`, and `waivers_json`. Its activities dispatch to `lane_repo_battery` on `venue205`. No repo_battery_205 run was active at inspection. A new measured receipt is still required; that older run is contract evidence only.

## D01.3 and S01.1 — summary projection and compaction allocation

Wall: `crates/norn/src/loop/{summarization.rs,summary_item.rs,summary_item_tests.rs,mod.rs}`, `crates/norn/src/session/context_edit.rs`, this queue, Cargo.toml/Cargo.lock, README and release notes. Provenance: D01 continuity/summary-input diagnosis; S01's measured raw-store clones; `ResponseItem` declares canonical JSON as lossless replay authority.

D01.3 omits only the typed reasoning/compaction item's nonempty top-level `encrypted_content` from the summary's plain-text transcript. It retains all other canonical fields, readable reasoning, tool arguments/results and unknown item kinds unchanged. An explicit summary-only marker names the omitted byte count; the item identity stays in the rendered JSON. No recursive field-name deletion and no provider replay or persistence change. Acceptance: actual summary request omits a large encrypted payload while original event and ordinary replay retain it exactly; null/absent/empty fields and a similarly named tool argument/unknown-item field remain unchanged.

S01.1 replaces whole-event-store clones in planning and commit validation with bounded borrows through `EventStore::with_events`, releasing the borrow before append. Cuts, IDs, context marks and persisted event shape remain identical. Existing plan mismatch, replay, cancellation and repeated-compaction tests cover semantics; inspect that neither method calls `store.events()`. No unmeasured performance percentage or storage saving is claimed. This reduces allocations, not duplicated on-disk compatibility fields.

D01.3/S01.1 local check: 4,714 core library tests passed, zero failed or ignored, including the actual summary request/replay preservation fixture and existing plan mismatch, tool-boundary, repeated-summary and cancellation tests. Strict release workspace/all-target Clippy, format and changed-file AST checks passed. New summary modules are scan-clean; old files' existing test-policy debt remains recorded. TUI source is unchanged; its preview.13 939-test result is retained as historical evidence, not claimed rerun. Logs: `var/verification/continuation-20260913/d01-projection-*`. Release build, actual-binary probe and installation remain next. Independent final-source review and exact-commit venue evidence are still required for landing.

## Installed preview.14 — 13 September 2026, 04:14 Melbourne

Installed from `9d19fc2711b9be85c1cb5f436dd3c41c864a9f71`, SHA-256 `f0c8356782e92da8487058bfa7175fc771080ce03cddad71370aad617c73c34a`. Release build and actual-binary Kitty keyboard/restoration probe passed. The atomic installer retained preview.13 for rollback and the Mac announcement completed. Receipt: `var/releases/preview.14-summary-projection/installation.json`. Removed 85,140,592 bytes of unused preview.13 core test executable after verifying no open handle; 24 GiB free at checkpoint.

Remote-main refresh corrected an earlier assumption: remote main already held preview.9 at `7ec7ee2`; only the local main checkout was stale at preview.8. Local main is now fast-forwarded to that already-published commit. Its pre-existing four-line planning link remains as the same uncommitted change, backed up under `var/main-refresh-20260913` and stash `f2f72ed8053602fc0f5e91e334aa4ac8780ac402`. No candidate landing or new main push occurred.

Pinned independent-review handoff: `var/reviews/preview14-20260913` includes PROMPT.txt, SCHEMA.json, SOURCE.tar.gz, DIFF.patch, FILES.json and SHA-256 MANIFEST.json. It reviews exact preview.14 source `9d19fc2` against remote main `7ec7ee2` (39 changed files). Tom has been asked to arrange the review he offered. No review verdict is claimed. The live venue battery `ea369582-aae2-4c22-8272-476133e9d524` measures preview.13 `39b5e636`; it cannot validate newer preview.14 code. It passed fmt/Clippy and reached later legs at last inspection. Continue D01.4 terminal-safe diagnostics while those independent checks proceed.

## D01.4 — terminal-owned diagnostics (in progress)

Provenance: D01 in NEXT-WORK-20260908; Tom's captured compaction warning overwrites the composer. Route tracing through a process-wide writer into a bounded, typed TUI channel while run_app owns the terminal. The runtime does not acquire terminal authority. Idle and in-flight loops retain compact expandable notices, including an explicit overflow notice. Capture ends after terminal restoration; unconsumed diagnostics are reported on stderr. Print/RPC diagnostics remain on stderr. No new file logger, polling task or provider event.

Wall: crates/norn-cli/src/diagnostics{.rs,_tests.rs}, print/tracing_setup.rs, lib.rs, tui/driver.rs; crates/norn-tui/src/diagnostics.rs, lib.rs, frontend_preferences{.rs,_tests.rs}, app/{diagnostics.rs,diagnostics_tests.rs,mod.rs,state.rs,event_loop.rs,turn/run.rs,frontend_preferences.rs}; four existing TuiInputs fixtures in tests/{pty_smoke.rs,model_selection.rs,support/mcp_channels_tui.rs,support/retained_workspace.rs}; README, Cargo version files, release notes and this queue. A launch-only tui.diagnostics.capacity override uses the existing CLI agent-event channel capacity when absent; it controls pending diagnostic count, not session retention or byte size. The setting remains unowned by interactive preference writes, which preserve it.

Acceptance: process-wide/native-thread diagnostics reach the typed receiver in order; no diagnostic writes reach stderr during terminal ownership; queue overflow is explicit; pre/post-terminal writes remain stderr; unread exit diagnostics and failed UI retention remain observable; settings reject zero/unknown keys. Strict Clippy, AST scans, focused CLI/TUI tests and actual executable terminal probe. This slice does not claim protection from arbitrary direct writes to stderr by unrelated code or children.

## D20.1 — PTY fixture exit protocol

The preview.13 exact-source venue receipt is red: all six declared legs executed, five green, tests red with 12 pty_smoke failures. Locally reproduced run_app_budgets_rows_on_small_terminal: the fixture sends one idle Ctrl+C and times out after five seconds. The actual TUI now requires two presses. Wall: crates/norn-tui/tests/pty_smoke.rs and support/retained_screen.rs if the terminal oracle requires a protocol update; release/queue evidence. Preserve assertions, timeouts and production confirmation semantics. Exit helpers must wait for visible confirmation before the second press; active cancellation assertions remain separate. Re-run the complete PTY target and strict Clippy; venue evidence must be re-run on the final source.

D20.1 fixture contract correction: wait for the accepted input frame (provisional publication alone does not retire the draft), still requiring the provider output to remain absent. Streaming cleanup waits for the completed response before idle exit. Narrow panes may clip the deadline suffix, so the exit helper observes the visible confirmation prefix. Resize waits retain both initial and final geometry for the frame oracle. No production acceptance or exit semantics are changed.

D01.4/D20.1 local verification: strict release workspace/all-target Clippy passed after the final capture-guard handoff fix. 574 CLI library, 941 TUI library and all 23 PTY smoke tests passed, zero failures/ignored. AST-grep compared all 20 changed Rust files against the preceding source: no added unwrap/expect or bypass/discard matches; existing policy debt is recorded separately. The core is unchanged from preview.14's 4,714-test run; no new core-suite execution is claimed. Actual executable Kitty/restoration check passed; the controlled retry-warning probe and install are the next checkpoints.

D01.4 visual wall extension: app/render/{screen_state.rs,transcript_items.rs,transcript_items_tests.rs}. The actual warning probe exposed that ordinary notice bodies expand by default. Diagnostic item IDs must carry frontend-only compact presentation membership, while keeping existing notice/error provenance and explicit expand controls. No body duplication or persisted schema change. Verify collapsed output hides the diagnostic body and explicit expansion retrieves the same body.

S01 next audit pointers (read-only): session/events.rs declares response_items authoritative and flat assistant fields compatibility projections; session/conversion.rs currently clones both into request messages, while jsonl_sink.rs serializes the entire event. Removing populated compatibility reasoning/tool fields requires checking action-log/child/replay readers and publication commitment digests first. No event has been rewritten, no compatibility field removed, and no on-disk space-saving claim is made by preview.15.

Final compact-notice renderer regression passed with all 942 TUI library tests. All 574 CLI library tests and 23 PTY cases were re-run after that change and passed, zero failures/ignored. The final strict Clippy pass and executable restoration check also passed. The actual warning probe now additionally requires the raw diagnostic body to be absent until expansion.

The final actual-executable HTTP-429 fixture made two loopback requests, displayed a collapsed warning, kept raw tracing output out of the owned terminal and restored the keyboard/screen on confirmed exit. No external provider request was made. Cleanup removed 422,076,656 bytes of verified older CLI/TUI cache executables after open-file checks; current binaries, core-test artifact, rollback copies and receipts remain. Source verification is complete for this slice; local installation and venue dispatch follow the source commit.

## S01.2 — one authoritative assistant representation in new writes

Provenance: S01 storage audit and D01 canonical summary projection. Nonempty response_items already owns replay, TUI bodies, text/tool accessors and token estimation. New canonical assistant events and their live/resumed provider Messages should retain that representation alone; flat content/thinking/reasoning/tool-call fields stay empty, while provider-neutral/legacy events retain their existing fields. Do not change serde rules or deserialize old data into a rewritten form: existing publication commitments hash every serialized field and must continue validating unchanged. Reduce fields before sealing new publication groups.

Wall: crates/norn/src/loop/runner/provider_call.rs, runner/tests.rs and runner/tests/canonical_storage.rs; session/{conversion.rs,events.rs,canonical_persistence_tests.rs}; provider/request.rs documentation; crates/norn-cli/src/commands/{session_export.rs,session_export_tests.rs}; Cargo version files, README/release notes and this queue. Markdown export must use canonical text/tool accessors before new writes stop duplicating those fields. No live JSONL rewrites, no new schema variant, no compaction of raw encrypted replay.

Acceptance: real runner writes canonical output with empty flat fields; byte measurement proves the duplicate ciphertext/tool arguments are absent from newly serialized rows; live and resumed Responses wire input is identical and preserves unknown fields/order, tool pairs and ciphertext; strict JSONL reload and V1 commitment validation succeed for both old duplicated groups and new compact groups. Legacy provider-neutral reasoning remains intact. Export shows canonical text and calls even when compatibility fields are empty or conflicting. Full core/CLI suites, affected TUI checks, strict Clippy, AST scans and repository-local build/resource discipline.

S01.2 wall extension: `crates/norn/src/session/canonical_transcript_tests.rs`. The first full core run identified two existing tests that require the redundant live-message projection. Preserve their authoritative-content and conflicting-call checks using canonical items and the event getters, and explicitly require empty duplicate fields under the new contract. No assertion about source retention or call identity is removed.

Preview.15 venue update: workflow `62dcf4da-c14c-44a7-8d16-7c1b2753a87c`, run `2c922ad9-23df-494c-9f80-cdd39c20d332`, completed 13 September 2026 at 05:19:36 Melbourne. Receipt verdict green for exact commit `53b4fec3d1bd74da61cc3f15f87bcd8a09fca6db`; all six declared legs passed. Receipt: `var/releases/preview.15-terminal-diagnostics/venue-result.json`. Independent review remains pending; this does not certify later storage edits or close the systemic edit/execute/restore source-binding issue.

S01.2 wall extension: `crates/norn/src/tools/agent/spawn/tests/persistence.rs`. The first core run completed with 4,713 passes and three failures: the two projection assertions above and a child-persistence assertion reading flat content. Update the child assertion to the authoritative text getter while retaining its exact canonical on-disk and provider replay comparisons. The new live/resume and old/new commitment regressions passed in that run.

S01.2 verification: 4,716 core, 577 CLI, 942 TUI and all 23 PTY tests passed with zero failures/ignored. Strict release workspace/all-target Clippy passed. The new export fixture initially omitted required canonical IDs and annotation/logprob arrays; both fixture failures were corrected without loosening the parser. The final exact live/disk/resume regression retained opaque reasoning, unknown fields and tool-call identity, and both old and new committed publication groups validated after disk reload without file mutation. A synthetic duplicated-row comparison measured 18,789 versus 9,820 bytes; this is not a measured reduction of whole real sessions. Release build, installation and final-source venue/review remain separately recorded.

S01.2 final local checks: release build, strict Clippy, all four test suites above, 11 changed Rust-file AST comparison, formatting and whitespace checks passed. The owned build cache shrank to 3.4 GiB after deleting 35 unopened preview.10–15 Norn library archives/metadata (1,509,014,011 bytes), positively identified by Cargo version depfiles. 16 GiB free at this checkpoint; installed/current/rollback executables and all session data retained. Preview.16 installation status is recorded in `var/releases/preview.16-canonical-storage/installation.json` once installed, and in the estate index, so status-only writes do not advance a source already dispatched for verification.

## D02.2 — carry scroll distance across earlier-page completion

Provenance: D02 retained-history acceptance, D02.1 demand at the loaded boundary, and the original request for smooth owned-screen scrolling. Preserve unserved backward row motion in the existing navigation owner while one asynchronous earlier-history page is pending. Repaint must neither consume that remainder twice nor start I/O. Resume only after the accepted oldest history cursor advances. Continue across as many bounded pages as the finite gesture needs; stop at the real beginning.

Direction reversal cancels unserved earlier motion, including a reversal in the same finite input batch. Latest, explicit selection/expansion, pane interaction and resize are navigation barriers: finish available motion and retire its deferred remainder. A late page may populate the same source's cache but cannot move the reader after that barrier. Failed or non-progressing earlier reads retire the gesture instead of automatically retrying. Source replacement retains the existing generation fence. No timers, polling, animation delay, second transcript owner, whole-history read or new service.

Wall: `crates/norn-tui/src/app/render/navigation.rs`, `navigation_tests.rs`, new `navigation_page_tests.rs`; `crates/norn-tui/src/app/transcript.rs`; `crates/norn-tui/src/app/view_actions.rs`, `view_actions/reading.rs`, `view_actions/mouse.rs`, `view_actions/commands.rs`; `crates/norn-tui/src/app/render.rs`; Cargo version files, README, this queue and release notes. Acceptance: actual frame preparation plus real asynchronous history tasks prove a multi-page scroll, no duplicate reads or repeat consumption while waiting, retained draft, real-start termination, direction-reversal/Latest/resize/selection cancellation and no retry after failure. Strict Clippy, AST scans, TUI/CLI/terminal checks and installed-binary smoke follow.

D02.2 barrier clarification: pointer movement without a pressed button is not an explicit selection/drag and must not cancel a waiting scroll. A regression covers that case. The initial unified workspace attempt passed 4,721 core tests with the CLI's AST feature union, then refused the runner's `--test-threads=6` argument in the custom MCP harness. Use `RUST_TEST_THREADS=6` and `CARGO_BUILD_JOBS=6` in the runner environment, as the venue does; do not weaken that harness or drop it from the run.

D02.2 verification wall extension: `crates/norn-tui/tests/retained_workspace.rs` and `crates/norn-tui/tests/support/retained_workspace.rs`. The full workspace run exposed a recorded-diff selection fixture racing two independent body reads: it locates the old-text row after arguments arrive but before the result adds its committed-evidence row. Wait for both actual argument and result evidence before taking drag coordinates. Keep all selection and copy assertions; no added delay or weaker predicate. Preserve the failed run.

D02.2 final local verification: 6,504 standard tests across 39 suites, 14 channel-stdio and four channel-TUI cases passed; zero failed or ignored. This includes 4,721 core, 577 CLI, 950 TUI unit and 23 PTY smoke tests. Strict release workspace/all-target Clippy (live-api-smoke compiled/linted only), ten-file AST comparison, formatting and release build passed. The earlier runner-argument refusal and diff-fixture race remain recorded in their original logs. No physical Herdr or paid live-provider acceptance is claimed. Cleanup after the build removed 344,065,477 bytes in eight positively versioned unopened old Norn libraries, leaving 12 GiB free and a 4.5 GiB owned cache. Preview.17 installation is the next action; preview.16 remains the current installed receipt.

## D02.3 — persisted compacted-history acceptance

Wall: new `crates/norn-tui/src/app/render/resumed_compaction_tests.rs`, its test-module declaration in `render.rs`, and this queue JSON/Markdown. Exercise the actual SessionManager create/close/resume path in an isolated repository-local fixture, then the retained-frame preparer and asynchronous history/body readers. Cross several earlier pages, expand a superseded old tool result, and return to Latest while retaining the composer draft. Prove original timeline bytes and event identities are unchanged and the old result remains readable after body-cache eviction. This is adapter/frame acceptance, not a physical-terminal or provider test. No production behaviour or version change is planned unless this check reveals a defect.

D02.3 verified: the actual SessionManager reopened 61 persisted events with two compactions. The tool result arrives on an earlier page before its call; after the final one-record page there is exactly one paired tool row. Expansion shows the original result, eviction followed by demand reload retains it, and Latest preserves the two-line draft. Browsing leaves original timeline bytes and event identities unchanged. All 951 TUI library tests passed, zero failed/ignored; strict release workspace/all-target Clippy (live-api-smoke compiled only), formatting and two-file AST comparison passed. Logs: `var/verification/continuation-20260913/d02-resume-{clippy,tui}.log`; the initial fixture's rejected ephemeral/persisted binding remains in `d02-resume-binding-fixture.log`. This is test-only source, not a new binary; preview.17 remains installed from 68802a0. Oversized/spooled results, descendant histories and physical-terminal acceptance remain explicit.

D03 source audit: `crates/norn/src/session/persistence/index_resolve.rs:113` uses the first matching name without checking duplicates or project scope. `crates/norn/src/agent/resume.rs:105` rebuilds tool completions through the live recording path; `crates/norn/src/session/action_log.rs:471` substitutes `Utc::now()` for their persisted completion times. Next work must preserve exact-ID/prefix behaviour, refuse ambiguous names with candidate IDs, apply the settled name scope, retain recorded timestamps and authoritative event order (including equal/backward timestamps). These are findings, not completed repairs.

Review state: `CLAUDE.md:36` requires Fable review. The attempt to launch read-only review of immutable preview.17 was rejected by automatic approval review: it requires explicit authorization to transmit that private source to Anthropic. Tom explicitly declined agent transmission and will arrange the review himself; no Fable process was launched. Reviewed commit and verdict await Tom. Preview.17 venue run 99c739bf-f305-4061-bc66-4c8817ce87df / bff3126e-fe31-4075-9118-2946c2939857 completed green for source 68802a046310784faff301db1f0277a68f4b87d9: all six declared legs returned zero. Receipt: `var/releases/preview.17-history-scroll/venue-result.json`. This receipt does not validate subsequent source changes. Continue unaffected implementation. Do not report an independent verdict or main landing. Cleanup removed another 276,964,672 bytes in four positively versioned, unopened obsolete test executables; `d02-resume-cleanup.json` records them. Current binaries, rollback files and sessions were preserved.

## D03.1 — preserve persisted action-log completion times

Wall: `crates/norn/src/session/action_log.rs`, `crates/norn/src/agent/resume.rs`, new `crates/norn/src/agent/resume_time_tests.rs`, Cargo version files, README, release notes and this queue. Add a replay-only entry point taking the persisted ToolResult timestamp through the existing action-log owner. Keep live dispatch/follow-up clock ownership unchanged and retain insertion order when timestamps tie or move backwards. Test exact timestamp precision through serialized event replay, outcomes/details and repeated reconstruction without additional entries or store writes. No fabricated time, on-disk migration, provider execution, or timestamp sorting. Name/project-scope resolution is the next separate D03 slice; this repair does not close it.

D03.1 local verification: 6,506 standard workspace/all-target tests across 39 suites and 14 channel-stdio scenarios passed, zero failed/ignored. This includes 4,722 core, 577 CLI, 951 TUI and 23 PTY tests. Strict release workspace/all-target Clippy with live-api-smoke compiled/linted only, formatting and a three-file AST comparison passed. The new regression checks serialized nanosecond precision, equal/backward timestamps, repeated replay, detail output and unchanged event-store bytes. Logs: `var/verification/continuation-20260913/d03-time-{clippy,workspace}.log`; AST receipt: `d03-time-ast.json`. Release build and installation remain pending at this source checkpoint.

D03.1 release build passed. All four channel-TUI scenarios passed as well. After the owned build ended, cleanup removed 344,106,166 bytes in eight positively versioned, unopened obsolete Norn library artifacts; receipt `d03-time-cleanup.json`. Disk observation: 8.1 GiB free, 5.3 GiB owned build cache. Actual-binary probe and installation follow the source commit; prior installed preview.17 remains retained until replacement succeeds.

## D03.2 — project-scoped names and explicit ambiguity

Wall: `crates/norn/src/session/persistence/index_resolve.rs`, new `index_name_tests.rs`, `index.rs`, `types.rs`; `session/manager.rs`, `session/manager/open.rs`, `session/manager/fork.rs`; `agent/session_spec.rs` and new `agent/session_name_tests.rs`; existing `session/provider_affinity_tests.rs` and `provider_affinity_stale_tests.rs` to pass the now-explicit lookup scope; `crates/norn-cli/src/commands/session.rs` and its new name-resolution test module; `runtime/from_cli.rs`; Cargo manifests, README, release notes and this queue. Provenance: NEXT-WORK-20260908 D03 and norn-cli DESIGN session commands.

Selection order remains full ID, exact name, then unique ID prefix of at least eight characters. IDs and prefixes remain usable across projects; only names are scoped to the explicitly selected working directory for agent resume/fork and their CLI aliases. More than one name match returns a typed ambiguity with every candidate ID; global storage inspection/removal APIs also refuse duplicate names. A name never automatically resumes a fresh launch. Empty scoped lookup means latest in that directory. Exact path equality remains meaningful for recorded directories that no longer exist; otherwise existing paths are canonicalized to handle symlinks. Missing paths cannot create a match; other filesystem errors must be propagated with their path. No process-global cwd inference inside the library, live-store migration or child respawn.

Acceptance: two projects with the same name select the requested project; duplicates within it refuse without opening/writing a timeline; global ambiguous lookup refuses; IDs/prefixes retain cross-project behaviour and minimum length; repeated names cannot hide a full-ID match; symlink equivalence and missing paths are explicit; real SessionRequest resume/fork and CLI wrappers use the same scope. Preserve provider-affinity and generation-revalidation tests. Run changed-file AST comparison, strict Clippy and relevant suites before installation.

D03.2 wall extension: `crates/norn/src/session/manager/tests/provider_state_validation.rs` and `crates/norn/src/session/persistence/timeline_tail_recovery/tests.rs` also call the internal affinity resume API. Pass each fixture’s recorded working directory; retain all rejection, byte-preservation and recovery assertions. Initial compile identified only these two missing scope arguments; preserved as `d03-name-clippy-scope-fixtures.log`.

D03 descendant-history audit while D03.2 verifies: `agent/assembly/tooling.rs:172-174` creates an action-log tree and registers only the live root; `agent/assembly/runtime/session.rs:17` rebuilds only the supplied root store. `session/action_log_tree.rs:36` still claims child branches are not persisted, but `session/persistence/index_entry.rs:109,115` records child paths and parent IDs. `norn-tui/src/app/session_replay.rs:18` loads the selected root transcript tail only. The next slice must discover registered descendant sources from the index, expose missing/unreadable coverage explicitly and load requested history on demand. It must not eagerly clone every child history at startup, register a completed child as a running agent, or respawn it. Selected child conversations and messaging continue under D13; historical access is not proof that an old recipient is live. These are current source findings, not a completed child-history repair.

Preview.18 venue workflow `034f3524-4f12-4fe2-925c-5eda1d30ecd3` / `cc674e54-0623-41d9-8abf-5793fae6d75b` completed green for exact `f525686c44b7dc7ea0fe6c3c3854e200118f35cd`: all six declared legs returned zero. Receipt: `var/releases/preview.18-replay-timestamps/venue-result.json`. This does not validate the newer D03.2 source. Independent review is still Tom’s responsibility; none has been launched by Ripley.

D03.2 first workspace run: core tests passed, including all seven new resolver/real-session cases. The new CLI fixture incorrectly placed its project folder inside an uninitialized session-store directory and was refused with MissingIndex. Keep the strict nonempty-directory refusal; put the fixture’s project and session-store directories beside one another. Preserve the failed run in `d03-name-workspace-fixture-layout.log`; no runtime policy changed for this repair.

D03.2 verification wall extension: `crates/norn-cli/tests/support/frontend_preferences_restart.rs`. The restart fixture asserted its answer immediately when Turn completed appeared, before the independently loaded retained answer body arrived. The captured output contains that body after the failed assertion. Wait for both the completed-turn notice and original answer in the same completed frame, as the existing multiline-composer fixture already does. Keep user-prompt colour, original text and composer assertions; no sleeps, weakened predicate, or production rendering change. Preserve the failure as `d03-name-workspace-restart-frame-race.log`.

D03.2 final tests: 6,514 standard workspace/all-target tests across 39 suites passed, zero failed/ignored, including 4,729 core, 578 CLI, 951 TUI and 23 PTY tests. Channel stdio 14, channel TUI four and frontend preference restart five also passed. Strict release workspace/all-target Clippy with live-api-smoke compiled/linted only, 17-file AST comparison and formatting passed. Logs: `var/verification/continuation-20260913/d03-name-{clippy,workspace}.log`; AST receipt: `d03-name-ast.json`. The release binary is building; actual-executable probe and installation follow.

D03.2 release build passed. Post-build cleanup removed 709,092,903 bytes in 48 positively versioned, unopened obsolete artifacts (six core libraries, 38 CLI/TUI libraries and four unit-test executables); current binaries, current test artifacts, dependencies, rollback copies and sessions remain. The three `d03-name-*-cleanup.json`/`d03-name-cleanup.json` receipts identify every file. Disk observation: 41 GiB free, 5.8 GiB owned build cache. Actual-binary probe and atomic installation follow this source commit.


## D03.3a — discover recorded descendants on demand

Wall: `session/branch.rs`, new `session/recorded_directory.rs` and `session/recorded_directory_tests.rs`; `session/persistence/index.rs`, new `index_descendants.rs`; `session/persistence/types.rs`; `tools/action_log.rs`, new `tools/action_log_branches.rs` and its tests; README, release notes and continuation JSON/Markdown. Paths without crate prefixes are under `crates/norn/src`.

Expose `action_log` query `branches` from the actual calling SessionBinding. One recovered-index transaction validates the caller generation and discovers only its registered subtree, in deterministic preorder with original sibling index order. Return persisted session IDs/generations, names, parents and working directories; do not fabricate runtime agent IDs, paths, liveness or messaging eligibility. Label coverage explicitly as registered index rows, with timeline readability and unregistered/ephemeral reservations not inspected. No startup work, directory crawl, child timeline reads, log reconstruction, mutation ledger or agent spawn. Ephemeral callers have an explicit separate result. Unexpected query filters are refused.

Acceptance: real root/child/grandchild records, a sibling and unrelated root; resumed root and child bindings see only their own subtrees; stale generations and reachable cycles refuse; missing child timeline remains a listed registration with no readability claim; no timeline writes or eager loads. This delivers discovery, not full descendant transcript inspection; D03.3b must add bounded selected-history access through the existing approved projection/body readers, then D13 connects selection to the TUI. D03 remains open.

Performance investigation P01: Tom reports slower response than Codex with the same model settings (13 September 2026, 07:39 Melbourne). Measure history/request construction, provider first-event latency and first visible output independently. Compare effective route/model/effort, input/cache tokens and tool schemas before attributing the delay to tree construction. No comparative performance claim yet.

P01 source anchors for measurement: `crates/norn/src/loop/runner/setup.rs:80` validates provider-state provenance from a cloned event snapshot; `loop/runner/prompt.rs:23` owns request assembly, per-iteration tool snapshots, dynamic prompt evaluation, replay validation and token preflight; `loop/runner/provider_call.rs:34` runs pre-LLM hooks before entering retry/provider transport. These identify boundaries to instrument, not measured bottlenecks. Compare matching provider routes before changing semantics.

Preview.19 venue workflow `8c3fee2d-64f7-4cf1-92dc-fed3933bb7af` / `4ff5f058-92a8-4b8d-a4d3-f4aa31cd49c4` completed green at 07:43:49 Melbourne on 13 September 2026 for exact `8349de1538f15d23b26f4343f653d68ceb3d1f04`. All six declared legs returned zero; receipt `var/releases/preview.19-resume-names/venue-result.json`. This does not validate uncommitted D03.3a changes. Tom owns independent review. D03.3a strict workspace/all-target Clippy has passed; the initial explicit-Arc-clone fixture finding remains recorded in `d03-directory-clippy-arc-fixture.log`.

## P02 — urgent responsiveness and flicker investigation

Tom reports intermittent severe UI unresponsiveness and flicker, 13 September 2026, and explicitly requires no shortcuts. Prioritize this after the current directory check completes, before further feature expansion. Preserve original styling and the owned-screen architecture. Inspect input-to-frame latency, frame preparation/publication, body/history completion fairness, and synchronous agent work sharing the UI future. Terminal capability negotiation and synchronized-output behavior must be observed through the actual terminal/multiplexer. A passing unit suite is insufficient visual acceptance; require terminal test-drive evidence. No root cause or repair is yet established.

P02 inspection: `crates/norn-tui/src/app/turn/run.rs:367-380` pins the agent step inside the same task and polls it before terminal input in a biased select. Synchronous setup/request work can therefore occupy the UI task; source alone does not quantify the reported delay. `render/frame/diff.rs:221` already publishes a prepared delta inside one synchronized-output envelope when capability detection enables it, and commits the baseline only after successful flush. Avoid replacing that with repeated clears. Need a deterministic busy-step/input-latency reproducer and observed capability/output trace before claiming a fix.

P02 test constraint: `crates/norn-tui/tests/pty_smoke.rs:79` deliberately hosts `run_app` in a current-thread runtime. Merely changing the turn future to `tokio::spawn` would not isolate synchronous work on that host. Any execution-owner change must preserve the full LoopContext and inbound queues, cancellation, child lifetimes and error recovery, and prove typing remains responsive during a deliberately busy synchronous provider/setup segment. Do not discard those owners or substitute empty context after an abnormal exit.


### P02.1 — reproduce synchronous turn work blocking input

Wall: `crates/norn-tui/tests/pty_smoke.rs`, new `tests/support/busy_input.rs`; `crates/norn-tui/src/app/turn/run.rs` and new `turn/worker.rs`; continuation JSON/Markdown, release notes and version files when installed. Reproduce a provider's synchronous preparation occupying the current-thread TUI host: send a draft while it is busy and require the composer to publish before that work finishes. This deliberately large test interval establishes isolation, not a frame-latency performance claim.

If confirmed, move the owned turn execution to the existing blocking executor with the runtime handle, keeping terminal input/rendering on their existing owner. Transfer and restore the exact LoopContext and inbound receiver on both normal and provider-error completion; cancel and join before propagating UI errors. A worker panic is a named fatal error, never a continuation with an empty context. Cancellation of the host must cancel the turn as well. No new daemon, terminal emulator, UI redesign or invented scheduler setting. Preserve current-thread runtime support, child/channel message delivery, tool cancellation and terminal restoration. Measure actual frame latency separately and investigate flicker independently.

D03.3a first workspace run: 4,732 core tests passed, including all new directory tests. The pre-existing schema metadata test still expected six query variants; add the new `branches` variant to its exact expected list. Failure retained in `d03-directory-workspace-schema-fixture.log`. No assertions removed.

P02.1 wall extension: `crates/norn-tui/src/error.rs` names fatal execution-worker join failures. Baseline reproducer failed in 2.08 seconds with no typed-draft frame while the synchronous provider occupied the UI task (`p02-busy-before.log`). The initial missing Write-trait import is preserved separately (`p02-busy-fixture-import.log`). The one-second live PID 95583 sample was predominantly idle and is only a baseline, not evidence of a live stall.

P02.1 execution-owner refinement: use one named, owned OS thread for the active turn, driving the existing runtime handle, rather than occupying its blocking pool for the entire turn. A long-lived pool occupant can deadlock a valid host configured with only one blocking slot when the turn itself needs off-executor persistence/body work. No new runtime or worker-count setting. The thread returns the exact execution state by oneshot and is joined before normal continuation or terminal-error propagation; dropping its UI owner requests turn cancellation. Test the current-thread host with one blocking slot.

The first off-UI attempt painted input in 1.85 ms in the synthetic case, but its completion assertion exposed a separate existing behavior: typing pins the current transcript body revision; admission can replace that provisional body and the view then shows a stale-selection notice. Preserve this as P02.2 rather than confusing it with execution cancellation. The isolation fixture will explicitly click Latest before checking the later cancellation result, retaining its original input-latency and draft-preservation assertions. Original failure: `p02-busy-after.log`.

P02.1 fixture correction: after its synchronous setup, the fake provider must remain pending for the explicit cancellation cleanup. Returning an already-complete zero-wait stream creates a different completion/cancellation race: the response completed and the UI correctly marked execution interrupted, so waiting only for the Cancelled variant timed out. Keep the cancellation assertion and have this busy-setup fixture await cancellation, as the existing child-result fixture does. The failed output is preserved in `p02-busy-completion-race.log`. Composer publication was 1.95 ms with a one-slot blocking pool; that is a synthetic observation, not full interactive performance acceptance.

P02.1 verification wall extension: new `crates/norn-tui/src/app/turn/worker_tests.rs` exercises the actual execution thread returning the original context and inbound receiver on provider success/error, plus fatal panic reporting. Busy-input regression now passes (1.84 ms observed input publication, 5.04 s full fixture including cancellation/terminal restoration) on a current-thread host with one blocking slot. Remaining work: strict checks, broader tests, installed executable and physical acceptance.

P02 further fairness audit: `app/turn/run.rs` drains agent-event broadcasts with `try_recv` until empty after a turn. Concurrent child producers can extend that drain. A finite admitted frontier should be exercised under a sustained child-event workload before changing the loop; this is not covered by the busy-provider reproducer. First P02 strict Clippy found similar local names and a map_err used only for observation; both are corrected without bypasses, with the original log retained as `p02-turn-clippy-naming-inspect.log`.

P02.1 verification wall extension: `crates/norn-tui/tests/model_selection.rs`. Its helper writes a provider prompt and `/view follow` back-to-back without waiting for opening-input admission. Off-task execution exposes the existing documented contract: the original draft remains until admission; newer edits are preserved, never erased by a stale receipt. Make these sequential model-policy probes await the submitted draft retiring before issuing their separate local navigation command. Preserve all exact provider-history, atomic refusal, model/budget and terminal-restoration assertions. Retain the failure as `p02-turn-workspace-command-admission-race.log`. This is fixture sequencing, not a fix for fast typing during pending admission; a separately editable next draft with exact rejection recovery needs an explicit composer design and regression coverage under P02.

P02.1/D03.3a final local verification: 6,521 standard tests across 39 suites passed, zero failures or ignored, including 4,733 core, 578 CLI, 953 TUI and 24 PTY. Channel stdio 14, channel TUI four and preference restart five also passed. Strict release workspace/all-target Clippy with live-api-smoke compiled/linted only, 16-file AST comparison, formatting and whitespace checks passed. Logs: `p02-turn-{clippy,workspace}.log`, `p02-turn-ast.json`. Release build and exact-executable probe/installation follow. Physical performance acceptance and the other P02 findings remain open.

Preview.20 release build passed. Post-build cleanup removed 515,696,046 bytes in 16 positively versioned, unopened obsolete Norn artifacts; receipts `p02-turn-cleanup.json`, `p02-turn-other-libraries-cleanup.json` and `p02-turn-old-tests-cleanup.json`. Sessions, current artifacts, installed executables and rollback copies preserved. Source commit/push and the actual-binary probe/atomic local install follow.

## P02.3 — delivered word/line movement in the actual composer

Tom reports Option/Control word/line skipping failure while the active typing investigation is underway. Installed Ghostty default keybinds emit `esc:b`/`esc:f` for Option+Left/Right; Norn previously handled only ALT arrow codes. CONTROL arrows fell through to single-character actions. Wall: `crates/norn-tui/src/input/keybindings.rs`, `composer_keys.rs`, `composer_keys_tests.rs`, `view_shortcuts.rs`, `view_shortcuts_tests.rs`; `crates/norn-tui/tests/iridium_composer.rs`; continuation JSON/Markdown, README, release notes, Cargo version/lock files. No Ghostty configuration or Iridium dependency change.

Route delivered ALT-B/F and CONTROL-Left/Right through existing Iridium word-motion verbs; preserve actual ALT arrows, Command arrows/Home/End line motion, Shift selections, repeats, cancellation and existing Ctrl+E thinking binding. Reserve new letter motions from conflicting view shortcuts. Prove actual terminal bytes in idle and active App phases, unchanged exact draft/provider history, Shift-selection replacement and terminal restoration. Ask which line keys the user means before remapping an established host binding. P02.2 pinned-frame stability remains higher-level unfinished work, not superseded by this input repair.

P02.3 baseline actual-App PTY failed at Ghostty Escape-B, after a 21.9 ms initial idle frame (`p02-modifiers-before.log`). Add delivered Command-arrow and Home/End coverage alongside Option/Control arrows, Shift-selection replacement, unchanged original text/history and both idle/active phases. Tom requested all combinations checked; Ctrl+E's current thinking-display binding is retained and explicitly documented, not silently repurposed. Preview.21 is the candidate version.

P02.2 current design constraint: settled retained-TUI design explicitly pins on typing; simply deleting automatic pinning would change the agreed reading behavior. The defect is that `app/render/transcript.rs` replaces the entire conversation with a stale-body notice when a pinned provisional BodyRef is superseded. Repair must preserve the actual last published reading content (using the existing typed frame/source maps), keep the composer live, retain exact original selection identity and distinguish displayed-snapshot copy from current source authority. Resize must reflow/reclip the retained content rather than reinterpret old coordinates. No replacement BodyRef, arbitrary body fetch, new session history copy or hidden switch to following. Build an actual App streaming/typing reproducer and a source-revision/resize selection regression before implementation. This remains open after the P02.3 keyboard repair.

P02.3 verification wall extension: `crates/norn-cli/tests/frontend_preferences_restart.rs`. The restart fixture used Alt+B as an arbitrary custom pane key, which now conflicts with the reserved Escape-B word motion. Replace only that fixture's arbitrary key with Alt+L, preserving exact Local/User/Run persistence and cross-project assertions. The new reserved-binding unit test separately requires Alt+B/F rejection. Keep the original failed run as `p02-modifiers-workspace-reserved-fixture.log`.

P02.3 all word/line terminal motions passed in the full run. The Ctrl+E test observed both idle visibility values, then its provider-reply wait was hidden below the deliberately pinned long status report. Use the real Latest mouse control while preserving the typed draft before submitting the active-phase fixture. Keep both idle/active visibility and exact provider-history assertions. Failure retained as `p02-modifiers-workspace-pinned-status-fixture.log`; this fixture navigation is not a claim to repair P02.2.

P02.3 final local tests passed: 6,525 standard tests in 39 suites, zero failed/ignored, including 4,733 core, 578 CLI, 955 TUI and 24 PTY. All 11 actual-App composer tests passed, including word/line movement and Ctrl+E in idle/active phases. Channel stdio 14, channel TUI four and preference restart five passed. Strict release workspace/all-target Clippy, seven-file AST comparison, formatting and whitespace checks passed. Logs: `p02-modifiers-{clippy,workspace}.log`, `p02-modifiers-ast.json`. Release build and actual-executable installation probe follow. Personal view bindings were inspected narrowly and have no Alt+B/F conflicts; no user settings were changed.


## P02.2 — retain the published reading view across body replacement

File wall: `crates/norn-tui/src/app/render.rs`, `app/render/{screen_state,transcript,reading_snapshot,reading_snapshot_tests}.rs`, `app/view_actions/latest.rs`, `crates/norn-tui/tests/iridium_composer.rs`, continuation JSON/Markdown, README, release notes and Cargo workspace/lock version. Paths abbreviated under `crates/norn-tui/src/` where clear.

R1: an actual App PTY must retain the visible response when typing pins it and the provider replaces its provisional body; the draft and caret remain usable. R2: retain only one successfully published conversation snapshot, bound to its source and logical anchor, with shared typed text; never grant old body references current authority. R3: resize reflows only retained displayed bytes/styles, without fetching bodies or interpreting old coordinates in the new geometry. R4: current-source copy remains revision checked; displayed snapshot copy is explicitly display scope. R5: explicit Latest, source replacement and a newly published current view retire the old snapshot; failed publication cannot create authority. Run focused source/resize/selection regressions, actual App PTY, AST, formatting, strict Clippy and full regression suite before a preview installation. No main landing without independent final-source review and exact-commit venue battery.

P02.2 reproducer evidence: the initial eight-row fixture pinned the input header and passed, so it did not prove revision-retirement coverage (`p02-reading-before.log`). A six-row fixture first omitted explicit following and correctly retained the input (`p02-reading-body-before.log`). The final fixture explicitly follows the active response, narrows to six rows, types a draft, and releases the provider. It fails with the whole-pane stale warning before the fix (`p02-reading-followed-before.log`) and passes afterwards (`p02-reading-after.log`). Two focused tests also pass for old-source refusal, displayed snapshot copy, grapheme/style reflow, failed flush and source replacement (`p02-reading-unit.log`). Final selection-preservation and full checks remain pending.

Checkpoint correction: preview.21 is already installed from `84f33f729e7678953f1aaa3063a77a230189070b` at 08:53:17 Melbourne on 13 September 2026. Its immutable receipt is `var/releases/preview.21-word-motion/installation.json`; main remains preview.9. Earlier pre-install paragraphs are chronological evidence, not current status.

P02.2 selection fixture correction: explicit `/view select` adds a feedback status row, leaving one retained body row in the six-row terminal. The first strengthened test wrongly expected the second, unpainted row to reappear on resize (`p02-reading-selection.log`). Publish that selection at seven rows so both asserted body rows really were visible; retain feedback and exact byte/style assertions. This is a fixture geometry correction, not permission to load offscreen snapshot content.

P02.2 first strict Clippy run refused three literal decomposed Unicode sequences in the new regression fixture. Use explicit `\u{301}` source escapes, retaining the identical decomposed runtime bytes and Unicode coverage; no normalization or lint allowance. Original log: `p02-reading-clippy-unicode-fixture.log`.

P02.2 final local regression suite passed: 6,528 standard tests across 39 suites, including 4,733 core, 578 CLI, 957 TUI, 24 PTY and all 12 actual-App composer tests; zero failed/ignored. Separate channel stdio 14, channel TUI four, frontend preference restart five, composer preference restart four and MCP launch ten passed. Strict release workspace/all-target Clippy (including compiled live-api-smoke) and seven-file AST comparison passed. Logs: `p02-reading-{clippy,workspace}.log`, `p02-reading-ast.json`. The release build and actual-executable probe/installation follow. This does not close physical Herdr acceptance or the other P02/P01 work.

P02.2 release build passed (`p02-reading-build.log`); formatting and whitespace checks are clean. Immutable review packaging, installed-binary probe and atomic local preview.22 installation follow this source commit. The endpoint at `http://127.0.0.1:8080/mcp` still refuses read-only run listing; no preview.22 venue start was attempted. Preserve the unresolved preview.20 start for read-only reconciliation, and keep main unchanged.


## P02.4 — progressive terminal capability replies

Wall: `crates/norn-tui/src/terminal/{caps,setup,setup_tests}.rs`, `crates/norn-tui/src/app/{helpers,event_loop}.rs`, `app/turn/mid.rs`, `crates/norn-tui/tests/{iridium_composer.rs,support/retained_workspace.rs}`, continuation JSON/Markdown, README, release notes, Cargo workspace/lock version. Repository-local verification/release scripts and receipts remain under `var/`.

R1: prove the installed CLI can paint its first idle frame after Primary DA alone, then accept late synchronized-output and Kitty replies without restart or increased startup wait. R2: share capability reduction between startup and idle/active terminal owners; unrelated/duplicate replies must not become input, provider work or redundant keyboard pushes. R3: synchronized output is usable only for a changeable Set/Reset mode, not NotRecognized/PermanentlySet/PermanentlyReset. R4: late keyboard negotiation pushes exactly once on the owned alternate screen, preserves parent mode, and retains cleanup ownership after uncertain writes/flushes; no pop without an owned push. R5: use existing frame delta publication and input batching, with no new polling, timer, full repaint, history cloning or ANSI renderer. Prove actual idle/active App behavior, exact input history, failure cleanup, strict Clippy/AST and full regression suite before local installation. Physical Herdr flicker acceptance remains separate.

Version-true dependency observation: Cargo.lock still selects termina 0.3.3. The earlier requested 0.4.0 update must be reconciled separately with Iridium's dependency, not assumed shipped. This capability slice uses the currently locked protocol shapes; it does not silently upgrade dependencies.

P02.4 baseline: `check_late_caps.py` launches the actual installed preview.22 without a prompt and with a loopback refusal-only provider. Primary DA alone permits its first unsynchronized frame; sending the sync and Kitty replies afterwards, followed by typing, fails at “late synchronization reply ignored after the first frame” (`p02-caps-cli-baseline.log`). The earlier probe refused before terminal admission because its compatible route omitted base_url (`p02-caps-cli-before.log`); the fixture now provides its own loopback URL. No claim is based on that setup refusal.

The implementation shares one capability reducer between admission and both input phases. Late keyboard setup uses the existing screen-local restoration owner with an idempotent compare-exchange; capability state is published only after a successful push/flush. Sync uses only changeable Set/Reset reports. Actual-App regressions deliberately disable and re-enable sync, deliver repeated Kitty replies, and verify ordinary draft/provider-history state in idle and active execution. A resize restores a full current frame for the existing strict screen oracle after the explicitly raw unsynchronized transport probe; the oracle is not weakened. Candidate version is preview.23.

P02.4 verification passed: 6,532 standard workspace/all-target tests in 39 suites, zero failed/ignored; 4,733 core, 578 CLI, 960 TUI, 24 PTY and 13 composer cases. Channel stdio 14, channel TUI four, frontend preference restart five, composer preference restart four and MCP launch ten passed. Strict release workspace/all-target Clippy (live-api-smoke compiled/linted only), eight-file AST comparison, formatting and production LOC passed. Changed production counts are caps 91, setup 176, helpers 87, mid-turn 195 and event loop 490; `p02-caps-loc.json`. Release build passed.

Actual-CLI proof now acknowledges the cursor after the typed character, excluding an unrelated repaint as evidence. Installed preview.22 fails; built preview.23 passes late synchronization, one keyboard push despite duplicates, exactly one cleanup pop, preserved parent state and zero loopback fixture provider requests. Per-run immutable probe copies, binary/probe/transport hashes and receipts are under `var/releases/preview.23-progressive-terminal/late-caps-*`; latest logs `p02-caps-cli-{baseline-timed,timed}.log`. First-frame samples varied: built candidate 1.600 s on its first run, then 0.189 s; installed baseline 0.096 s in the paired probe. These are isolated observations, not a controlled latency comparison or a physical smoothness verdict. P01 measurement remains open; no startup wait was added. Installation follows this source commit.

Next P02 source evidence: `app/turn/run.rs:412-424` drains live events until Empty, so concurrent child producers can extend that work before input resumes. Inspect the adjacent active-input, publication and history completion loops too; do not fix one drain while leaving another unbounded UI-owner await. Preserve exact root completion history and channel/input receipts.


## P02.5 — fair turn-completion handover (in progress)

Wall: `crates/norn-tui/src/app/turn/{run,completion_events,completion_events_tests}.rs`, this continuation document and its JSON status; repository-local verification artifacts under `var/verification/continuation-20260913/`. Extending the wall for the terminal-aware receipt/checkpoint/history phase requires naming those files here before editing.

R1: capture the live broadcast receiver frontier once after execution joins; subsequent child publications cannot extend this drain. Keep all later retained events in the same receiver, in order. Account for a lag notification by its actual skipped-message count, never as one consumed event. Preserve the existing explicit lag/closure/error handling. R2: deterministic tests publish more events during consumption and exercise lag across the captured frontier, callback failure, empty and closed receivers. R3: audit adjacent completion waits and make them terminal-responsive before declaring P02.5 complete or installing its completed preview. A finite broadcast drain is only the first source change, not a complete handover fix.

Adjacent ownership audit: active-input deliveries come from the joined root runner, so its final receipt set is finite. Publication reconciliation visits the retained attempt map synchronously and creates no producers. Exact input reads (`input_tasks.join_next`), cancellation repair, checkpoint and paged history currently await on the terminal owner without an input branch. Those waits remain the next part of P02.5. Before servicing Submit during them, close the finished execution's active-input receiver: the existing `ActiveInputError::Closed` path then retains submitted text as a follow-up instead of accepting it into an abandoned queue. Preserve pending steer acknowledgements and the exact final root-history reconciliation.

Keyboard follow-up, 13 September 2026: Tom confirms Option is now working after testing directly in Ghostty and through Herdr/Manifold. Installed preview.23 key-sequence probe and actual-App idle/active motion and Control+E tests passed. Control+E remains the thinking toggle; no terminal settings were changed. This closes the reported Option failure only, not physical performance acceptance or the shortcut-conflict work.

P02.5 wall extension: add `app/turn/completion_wait.rs` and `crates/norn-tui/tests/{iridium_composer.rs,support/retained_workspace.rs,support/completion_checkpoint.rs}` for terminal-aware settlement and an actual-App checkpoint barrier. Reuse the existing render clock and mid-turn editor handling; close the completed runner input receiver before accepting more text. Resolve/requeue prior steers before new completion-phase submissions so new follow-ups cannot overtake those pending inputs. No new worker, polling timer or runtime setting.

P02.5 wall extension: `app/turn/mid.rs` receives a regression test for Enter after the completed runner receiver closes: retained prior steers precede the new follow-up, which is queued exactly once and never marked delivered. Production key bindings remain unchanged.

P02.5 release wall extension: workspace Cargo.toml/Cargo.lock version, README and `docs/release-notes/UNRELEASED.md`; candidate preview.24. Baseline proof: the strengthened actual-App checkpoint fixture with the pre-change HEAD turn orchestrator fails after 15.03 seconds waiting for the typed frame (`p02-completion-checkpoint-baseline.log`, exit 101). The checkpoint barrier explicitly refuses a proof after timeout or release. Candidate source was restored by the runner's finally block. Initial candidate checkpoint test passed; final strengthened fixture and strict/full regression verification follow.

Settlement now keeps its future pinned while serving terminal events and existing view/preference/diagnostic/voice job completions, reusing the turn render clock. Even immediately ready history pages first service one already queued terminal event. The joined runner receiver closes before this phase; prior unacknowledged steers are requeued first. The final exact-input read set moves into a local task owner so the editor can borrow AppState while reads finish. Terminal failure remains an error; no persistence error or history page is discarded.

P02.5 final regression verification passed: 6,539 standard workspace/all-target tests across 39 suites, zero failed/ignored: 4,733 core, 578 CLI, 966 TUI, 24 PTY and 14 composer cases. Separate channel stdio 14, channel TUI four, frontend preference restart five, composer preference restart four and MCP launch ten passed. The strengthened held-checkpoint fixture confirms the sink has neither returned nor timed out before accepting typed/navigation/resize frames; exact provider/store census is unchanged. Prior-orchestrator baseline failed after 15.03 seconds. Strict release workspace/all-target Clippy (live-api-smoke compiled/linted only), eight-file AST comparison, formatting, whitespace and production LOC passed (run 471, mid 195, completion wait 101, completion frontier 20). Logs: `p02-completion-{clippy,workspace}.log`, `p02-completion-frontier-ast.json`, `p02-completion-loc.json`. All recorded build-input hashes remain unchanged. Release build, immutable package and local installation follow; no physical smoothness or main-landing claim.


## P02.6 — independent next draft during opening-input admission

Initial wall: `crates/norn-tui/src/input/{mod,composer_transactions,composer_draft,composer_draft_tests}.rs`, continuation JSON/Markdown. This is the ownership foundation; App admission/recovery controls will be named in a wall extension before edits. R1: move the pending draft's existing kernel, selection, undo tree and recall cursor into an opaque owner; create an immediately editable blank composer with the same resolved configuration and a distinct document identity. R2: keep exactly one submission-recall store; do not clone or reopen it when changing drafts. R3: accepting a detached original may append only that validated original to recall and must not clear the current draft. R4: exchanging drafts retains both kernels, exact byte/cursor/undo states and current editor configuration. R5: stale or foreign snapshots fail before either ownership or recall changes. R6: integrate the real opening-input receipt and a visible rejection recovery action, then prove accepted/rejected pending admission, ongoing typing, undo, repeated Submit, exact provider history and receipt failures in the actual App before local installation. The existing undo-after-acceptance behaviour for an untouched composer remains part of the integration acceptance.

Recovery UX proposal sent to Tom: keep a newer draft in place and show a visible Recover rejected message action; recovery must retain the newer draft too. No failed input is automatically resent or inserted into accepted-input recall. Default to that non-overwriting behaviour if Tom has no preference.

Installed checkpoint correction for this new source slice: preview.24 is installed from `f897bda22f1a5648535d574e4f4dc46e7ee496e8`, receipt `var/releases/preview.24-completion-input/installation.json`, 13 September 2026 10:27:31 Melbourne. All 6,539 standard tests and strict Clippy/AST passed. Tom subsequently requested that updates use Dot or his requested messaging channel, with no further Mac say announcements.

P02.6 ownership foundation verified: 6,543 standard workspace/all-target tests across 39 suites passed, zero failed/ignored, including four new draft-ownership tests and the existing actual-App integration suite. Strict release workspace/all-target Clippy (live-api-smoke compiled/linted only), four-file AST comparison, formatting and production LOC passed (draft owner 48, transactions 322). Logs `p02-drafts-ownership-{clippy,workspace}.log`; AST `p02-drafts-ownership-ast.json`; LOC `p02-drafts-ownership-loc.json`. This source does not yet alter submission UX and is not a new local installation. Tom explicitly selected “Keep new draft; offer recovery.” Next: App admission/receipt wiring and visible recovery, with exact recovery of both draft owners and untouched-composer undo preserved.

P02.6 App/recovery wall extension: `crates/norn-tui/src/app/{mod,state,composer_submission,composer_submission_tests,composer_recovery,composer_recovery_tests}.rs`, `app/render{.rs,/screen_state.rs,/composer.rs,/composer_tests.rs}`, `app/view_actions/{latest,mouse}.rs`, `tests/{iridium_composer.rs,support/retained_workspace.rs,support/completion_checkpoint.rs}`, continuation JSON/Markdown and release notes. The recovery control uses the existing footer, only a successfully published frame grants a click, and stale/source-replaced/failed frames cannot recover a different draft. Saved drafts cycle through owned kernels without an invented count cap or a second recall store. Rejection restores an untouched composer immediately; if changed, it keeps the new draft and offers Recover rejected message. After recovery, Switch saved draft restores the displaced newer draft. These are in-memory drafts, not a new session persistence claim.

## U03 — child activity belongs to the Agents side pane; pane-scoped copy

Tom, 13 September 2026 10:50 Melbourne: the screenshot shows repeated Child spawn/... tool-result and Provider completed notices flooding the parent conversation. Routine child activity belongs in the tree; the tree belongs in the side pane. Selecting/copying only one pane must work. Source finding: `app/dispatch.rs` already updates the existing agent status and activity log, but `app/notices.rs::child_event` duplicates those events into the root transcript. `app/render/agents.rs::allocate` still reserves full-width rows above the composer. The displayed clipboard error is specifically the composer clipboard path with transport Unspecified; it is not evidence that selection in another pane reached the intended copy action.

Wall: `crates/norn-tui/src/app/{notices,dispatch,render,composer_effects}.rs`, `app/render/{agents,agents_tests}.rs`, `app/view_actions/{keys,shortcut_commands}.rs`, `app/{display_selection_tests}.rs`, `crates/norn-tui/tests/{pty_smoke,retained_workspace}.rs`, `tests/support/retained_workspace.rs`, README, release notes and continuation docs. Route routine child events only through their existing status/activity owner; preserve attributed failures and actual final child-result delivery. Render the tree only in the existing Agents pane, preserve the composer and conversation height, and retain explicit pane controls/preferences. Prove selection stays inside the pane and the requested copy path cannot copy the composer instead. Do not claim clipboard acceptance from an OSC52 write alone. No provider/agent routing changes, new event log or eager child-history loads. Full selected-child conversation/message access remains D13.

P02.6 and U03 candidate verification, 13 September 2026 Melbourne: 6,550 standard workspace/all-target tests across 39 suites passed, zero failed/ignored (core 4,733, CLI 578, TUI 974, composer PTY 16, general PTY 24, retained-workspace PTY eight). Separate channel stdio 14, channel TUI four, frontend preference restart five, composer preference restart four and MCP launch ten passed. Actual-App barriers prove continued typing and resize during held opening admission, one original accepted provider request, no automatic rejection resend, clickable recovery/switch preserving both drafts, and explicit later submission. The first fixture attempt incorrectly awaited new response text in a deliberately pinned conversation; the final fixture explicitly clicks Latest without changing draft or provider-count assertions. Logs retain both attempts.

U03 actual-App tests open the Agents side pane and observe child activity there. Repeated child tool/provider events update the existing activity owner without adding parent transcript notices. The tree no longer reserves full-width rows above the composer. A real pane drag followed by terminal-reported Command+C and configured Option+C yields exactly two OSC52 payloads containing only the selected pane text. The pane/composer clipboard routing retains the draft. This does not establish physical Ghostty/Herdr clipboard acceptance.

Strict release workspace/all-target Clippy (live-api-smoke compiled/linted only), 22-file AST comparison, production LOC, formatting and whitespace passed. Logs `p02-drafts-u03-final-{clippy,workspace}.log`; AST `p02-drafts-u03-ast.json`; LOC `p02-drafts-u03-loc.json`; exact build-input hashes `p02-drafts-u03-source-hashes.json`. A Clippy if-not-else finding was fixed structurally before the passing run. Preview.25 release build and exact-executable probes passed; atomic local installation is recorded at 11:12 Melbourne in `var/releases/preview.25-drafts-agent-pane/installation.json`. Tom closed Dot at 11:00:55 Melbourne; updates stay in this conversation, no Mac say. Main/review/venue and physical performance acceptance remain separate.

## D13.1 — read-only access to a selected live child's actual history

The installed checkpoint is preview.25 at `d6ad384bf355da3cf1956c91c791826d96b751f5`, installed 13 September 2026 at 11:12 Melbourne. Its receipt is `var/releases/preview.25-drafts-agent-pane/installation.json`; the final-source review package is `var/reviews/preview25-20260913/`, for Tom to submit. Main remains preview.9.

Wall: `crates/norn/src/session/{store,action_log,action_log_tree,action_log_tree_history,action_log_tree_history_tests}.rs`, `session/store/{history_page,history_reader}.rs`, and this continuation JSON/Markdown.

R1: Expose a sealed read-only handle to the existing EventStore with its actual bound source. No copied store, new session, append capability or execution authority. R2: Require registered caller and selected target, limit selection to the caller's subtree and compare the bound agent/parent with the tree registration. Unbound and miswired sources fail explicitly. R3: Use existing approved history/body readers and explicit page/byte demand, preserving cursor/source/generation validation. R4: Readers observe accepted future appends; retained readers stay with their original source across root rotation. R5: Cover sibling/ancestor/unknown denial, unbound retry, source/body isolation, future appends and root replacement.

This is the shared read capability needed for selected conversations, not completion of D03.3b persisted descendant access or D13 TUI selection/messaging. No disk work or history projection belongs on the render/input path.

D13.1 wall extension before editing: `crates/norn/src/agent/assembly/tooling.rs`, `tools/agent/delegation.rs`, `tools/agent/spawn/execute.rs`, `tools/agent/fork_tool.rs`, `integration/rhai/agent_ops/spawn.rs`. R6: the audit found only the TUI root currently binds a view source; bind the actual source in root assembly and the shared child allocation path (spawn, fork, Rhai). Do not mint attribution in the reader. Existing direct constructors may remain explicitly unbound until their owner binds them.

D13.1 verification wall extension: `crates/norn/src/agent/assembly/runtime/tests.rs`. Strengthen the actual root-infrastructure fixture to share the production event store with its action log and assert that the selected reader observes writes to that same store.

External Manifold handoff relayed by Tom during D13.1: its owner reports two account-rotation fixes, stage-specific failure reporting committed on the 205 builder and bounded incremental output matching still awaiting full-suite completion/commit. Neither was landed in Tom's local main or installed at that handoff. This is the other agent's report, not independently verified here, and does not establish Norn typing/flicker performance. The earlier Manifold spine design remains an unapproved draft.

D13.1 final local verification: 6,555 standard cases across 39 suites passed, zero failures or ignored. Strict release workspace/all-target Clippy passed with `live-api-smoke` compiled/linted only. Five selected-history regressions passed, and the actual root assembly fixture confirms the reader sees the same runtime store. AST comparison covers 13 changed Rust files; production LOC covers 11 files, maximum 469. Format and whitespace checks passed. Logs: `d13-history-final-{clippy,workspace}.log`; `d13-history-source-hashes.json` matches the tested Rust sources. The initial redundant-clone finding is preserved in `d13-history-clippy.log` and fixed without a bypass. This is verified core access, not an installed pane-selection feature; preview.25 remains installed. D03.3b/D13 TUI and messaging, the full programme, physical performance acceptance, independent final-source review and the exact-commit venue receipt remain open.

## D13.2 — separate displayed conversation from runtime ownership

Wall: `crates/norn-tui/src/app/{mod,conversation_view,conversation_view_tests,view_actions}.rs`, `app/render/{transcript,navigation,reading_snapshot,hit}.rs`, continuation JSON/Markdown. R1: rendering and navigation borrow only a source-matched Transcript/ScreenState plus display toggles, without provider, runtime, composer or command authority. R2: retain the existing styled renderer, snapshots, exact selection and finite-batch navigation; no event-store duplication. R3: refuse mismatched sources before any mutation; a selected child must use the same renderer while the root transcript, draft and viewport stay intact. R4: test two actual stores, approved bounded pages/bodies, independent navigation and source-isolated selection. R5: root TUI regressions must remain green. This extraction is a prerequisite within D13.2; selecting the view, supervising its reads and live refresh, and messaging remain open until actual-App integration proves them.

D13.2 wall extension: `app/render/reading_snapshot_tests.rs`, to pass an explicit borrowed view to the same existing snapshot assertions.

D13.2 presentation extraction verification: 6,558 standard tests across 39 suites passed, zero failed/ignored. Strict release workspace/all-target Clippy passed (live-api-smoke compiled/linted only). AST comparison covers nine Rust files, production LOC seven files (maximum 402). Three new regressions use actual source-bound stores and the shared renderer to prove independent child painting/navigation and source-isolated selection, preserving the root draft and viewport. Logs: `d13-view-final-{clippy,workspace}.log`. This verifies the presentation extraction only: actual pane selection, read supervision, live refresh and messaging remain in progress; preview.25 is still installed.

D13.2 read-owner wall extension before editing: `app/{transcript,session_replay,render,event_loop,slash}.rs`, `app/transcript/{read_access,read_access_tests}.rs`, `app/turn/run.rs`, `app/view_actions/{reading,reading_tests,latest_tests}.rs`, `app/render/{navigation_tests,navigation_page_tests,resumed_compaction_tests,transcript}.rs`. R6: each conversation owns its matching sealed read handle; display demand never accepts a writable runtime store. Store binding checks occur off the terminal executor once at source acquisition. R7: missing/mismatched access fails before pending-state mutation; preserve bounded coalesced task supervision. R8: verify real async history/body work, foreign-source refusal and future appends, plus existing App/navigation/input regression coverage. This is the read owner used by the upcoming selected view, not a second history store.

D13.2 read-owner verification: 6,561 standard tests across 39 suites passed, zero failed/ignored. Strict release workspace/all-target Clippy passed (live-api-smoke compiled/linted only). AST comparison covers 15 Rust files; production LOC covers nine files, maximum 490. New async regressions prove missing/foreign access refuses before pending-state mutation, attaching the correct reader permits retry, body requests remain bounded/coalesced, and independent conversations retain source identity while observing future appends. Existing resumed-compaction navigation and actual-App suites passed. Startup and `/new` acquire the sealed reader off the terminal executor; ordinary display demand no longer takes a writable store. Logs: `d13-read-owner-final-clippy.log`, `d13-read-owner-workspace.log`; `d13-read-owner-source-hashes.json`. Initial compile diagnostics (one removed import and three fixture destructures) remain in `d13-read-owner-clippy.log`; no bypasses added. Actual agent selection, live refresh and messaging remain unfinished. Preview.25 remains installed; disk check reports 83 GiB free and no artifacts removed for this slice.

D13.2 completion-supervision wall extension before editing: `app/{mod,state,read_tasks,read_tasks_tests}.rs`, `app/turn/completion_wait.rs`, plus the existing D13.2 transcript/read-access, read/render/navigation fixture and event-loop wall. R9: a single frontend owner supervises all explicitly admitted history/body tasks across view changes and source rotation. Captured source must be checked before a completion can alter navigation/search state. No per-frame scan of child task sets, additional polling/worker actor or history copy. Cancellation/foreign completion regressions must preserve the newly displayed source.

D13.2 frontend read supervision verification: 6,563 standard tests across 39 suites passed, zero failed/ignored. Strict release workspace/all-target Clippy passed (live-api-smoke compiled/linted only), AST comparison covers 19 Rust files and production LOC 11 files, maximum 490. Two new AppState regressions prove accepted history/body tasks survive root projection replacement without mutating the new view or draft, and an old-source failure cannot clear a real newly requested older-history search or Latest intent. The independent two-conversation test now shares one read supervisor and routes completions by their captured source. Search waits only for its conversation’s body demands. Logs `d13-read-tasks-final-clippy.log`, `d13-read-tasks-workspace.log`; initial if-not-else finding retained in `d13-read-tasks-clippy.log`, fixed structurally. This is not an actual agent-click or physical-terminal acceptance claim.

D13.2 selected-view ownership for the next integration: retain the root transcript/runtime publication as a fixed owner; separate source-specific presentation/bookmarks from the one physical terminal frame owner. Child views reuse ConversationView and a sealed read handle; the existing frontend read_tasks supervisor routes completions by captured ViewSource. Preserve per-agent draft owners and human attribution for messaging. Do not swap AppState transcripts around provider calls, create another raw history store, or scan child task sets on every key/frame. Actual selection, child refresh and messaging remain unfinished; preview.25 is unchanged.
