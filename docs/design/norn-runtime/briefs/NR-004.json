{
  "id": "NR-004",
  "cluster": "norn-runtime",
  "title": "Session persistence — JSONL file management and dual persistence",
  "depends_on": ["NR-001"],
  "blocked_by": [],
  "checklist": ["C32", "C33", "C34", "C35", "C36", "C37", "C38", "C39"],
  "stories": ["S2", "S16", "S19"],
  "purpose": "Wire Norn's JsonlSink-backed EventStore into the assistant service so that session conversation history persists to disk and can be replayed on resume. Establish the dual persistence model: JSONL files for Norn's conversation continuity, PG events for the UI's history display. Both coexist independently.",
  "task": "Create a session file manager that handles path resolution, directory creation, EventStore construction for new/resume/fork modes, and JSONL file lifecycle. The session file path convention is {data_dir}/sessions/norn/{session_id}.jsonl. spawn_norn_loop calls this manager to obtain an EventStore before building the AgentBuilder. The existing PG persistence (persist_events) and Redis streaming (stream_deltas_to_redis) remain unchanged — they subscribe to the AssistantEvent broadcast channel.\n\nDependency setup: No new cargo dependencies. The norn crate (JsonlSink, EventStore, SessionEvent) and tempfile are already dependencies of meridian-services.",
  "requirements": [
    {
      "id": "R1",
      "title": "Session directory and path resolution",
      "spec": "The system SHALL resolve Norn session JSONL file paths as {data_dir}/sessions/norn/{session_id}.jsonl where data_dir is the server's configured data directory. THE SYSTEM SHALL create the sessions/norn/ directory if it does not exist. THE SYSTEM SHALL NOT use ~/.norn/ (norn-cli local convention).",
      "acceptance": [
        "Session files are at {data_dir}/sessions/norn/{session_id}.jsonl",
        "Directory is created automatically if missing",
        "data_dir comes from server configuration (ServiceContext or ServerConfig)",
        "Path resolution is a pure function testable without filesystem"
      ],
      "files": {
        "create": ["crates/meridian-services/src/assistant/norn_session_store.rs"],
        "modify": ["crates/meridian-services/src/assistant/mod.rs"]
      },
      "checklist": ["C33"],
      "stories": ["S13", "S16"]
    },
    {
      "id": "R2",
      "title": "EventStore for new sessions",
      "spec": "WHEN a new Norn session starts (SessionStartMode::New), THE SYSTEM SHALL create a JsonlSink at the session file path in write mode AND construct EventStore::with_sink(sink) with empty initial events.",
      "acceptance": [
        "New session creates a fresh JSONL file",
        "EventStore has no initial events",
        "JsonlSink is in write (create) mode",
        "The session file path follows the {session_id}.jsonl convention"
      ],
      "files": {
        "modify": ["crates/meridian-services/src/assistant/norn_session_store.rs"]
      },
      "checklist": ["C32", "C35"],
      "stories": []
    },
    {
      "id": "R3",
      "title": "EventStore for resumed sessions",
      "spec": "WHEN an existing Norn session resumes (SessionStartMode::Resume), THE SYSTEM SHALL read the JSONL file at the session path, deserialize SessionEvent lines into a Vec<SessionEvent>, create a JsonlSink in append mode for the same file, and construct EventStore::with_sink_and_events(sink, events). IF the JSONL file does not exist, THE SYSTEM SHALL fall back to creating a new session (EventStore::with_sink, no error).",
      "acceptance": [
        "Resume reads existing JSONL and replays events",
        "JsonlSink is in append mode (does not truncate existing content)",
        "EventStore receives pre-populated events from the file",
        "Missing file falls back to new session behavior (no error, clean start)",
        "Corrupt lines are logged and skipped, not fatal",
        "Truncated trailing line (incomplete JSON from power failure mid-write) is treated as corrupt and skipped",
        "Empty file (0 bytes) falls back to new session behavior",
        "File containing valid JSON that is not SessionEvent is treated as corrupt per line"
      ],
      "files": {
        "modify": ["crates/meridian-services/src/assistant/norn_session_store.rs"]
      },
      "checklist": ["C34"],
      "stories": ["S2"]
    },
    {
      "id": "R4",
      "title": "EventStore for forked sessions",
      "spec": "WHEN a session is forked (SessionStartMode::Fork), THE SYSTEM SHALL copy the source session's JSONL file to a new path (new session ID), then construct an EventStore from the copy using the resume flow. IF the source file does not exist, THE SYSTEM SHALL create a new session instead.",
      "acceptance": [
        "Fork copies source JSONL to {new_session_id}.jsonl",
        "New file is independent from source (copy, not symlink)",
        "EventStore is built from the copy with resume semantics",
        "Missing source file falls back to new session"
      ],
      "files": {
        "modify": ["crates/meridian-services/src/assistant/norn_session_store.rs"]
      },
      "checklist": ["C36"],
      "stories": []
    },
    {
      "id": "R5",
      "title": "AgentBuilder receives EventStore",
      "spec": "WHEN spawn_norn_loop builds the AgentBuilder, THE SYSTEM SHALL call .session(store) with the EventStore obtained from the session file manager. THE SYSTEM SHALL NOT call .session() with a bare EventStore::new() (the bug in make_norn_step_runner).",
      "acceptance": [
        "AgentBuilder::session(store) is called with a JsonlSink-backed EventStore",
        "EventStore::new() is never used for member-facing Norn sessions",
        "Session events are written to disk as the agent runs"
      ],
      "files": {
        "modify": ["crates/meridian-services/src/assistant/service.rs"]
      },
      "checklist": ["C32"],
      "stories": []
    },
    {
      "id": "R6",
      "title": "PG and Redis persistence unchanged",
      "spec": "The existing persist_events and stream_deltas_to_redis tasks SHALL continue to subscribe to broadcast::Sender<AssistantEvent> without modification. WHILE a Norn session runs, THE SYSTEM SHALL have both JSONL persistence (Norn-internal) and PG/Redis persistence (AssistantEvent broadcast) active simultaneously.",
      "acceptance": [
        "persist_events spawned for Norn sessions exactly as for Claude Runner sessions",
        "stream_deltas_to_redis spawned for Norn sessions exactly as for Claude Runner sessions",
        "Both subscribe to the same broadcast channel that the Norn session loop writes to",
        "No modifications to persistence.rs or streaming.rs"
      ],
      "files": {},
      "checklist": ["C37", "C38"],
      "stories": ["S19"]
    }
  ],
  "dependency_setup": "No new cargo dependencies. The norn crate (JsonlSink, EventStore, SessionEvent) and tempfile are already dependencies of meridian-services.",
  "boundaries": [
    "SHALL NOT modify persistence.rs or streaming.rs",
    "SHALL NOT modify the EventStore or JsonlSink implementations in the norn crate",
    "SHALL NOT modify the JSONL format — use Norn's existing SessionEvent serialization",
    "SHALL NOT implement cleanup/garbage collection of old session files (future scope)"
  ],
  "verification": [
    "New session creates a fresh JSONL file at the correct path",
    "Resume reads an existing JSONL file and pre-populates the EventStore",
    "Fork copies the source file and builds from the copy",
    "Missing source file for resume/fork falls back to new session without error",
    "AgentBuilder::session() is called (not bare EventStore::new())",
    "PG events appear in assistant_session_events for Norn sessions",
    "Redis stream receives delta events for Norn sessions"
  ]
}
