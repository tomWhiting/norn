{
  "id": "NR-008",
  "cluster": "norn-runtime",
  "title": "Workflow step runner fix and integration verification",
  "depends_on": ["NR-004"],
  "blocked_by": [],
  "checklist": ["C40", "C41", "C66", "C67", "C68", "C69", "C70", "C71", "C72", "C73", "C74"],
  "stories": ["S15", "S17", "S19", "S21"],
  "purpose": "Fix the make_norn_step_runner session persistence gap (events lost when workflow steps complete) and verify the complete integration: WebSocket compatibility, existing test preservation, event translation pipeline tests, namespace tool tests, session persistence tests, and line count compliance.",
  "task": "Wire .session(store) in make_norn_step_runner with workflow-scoped EventStore at {data_dir}/sessions/workflow/{step_id}.jsonl. Verify that the WebSocket handler, session replay, and resubscribe mechanism work unchanged with Norn sessions. Add integration tests covering the full event translation pipeline, namespace tool actions, and session persistence flows. Verify no file exceeds 500 lines of code and clippy passes clean.",
  "requirements": [
    {
      "id": "R1",
      "title": "Fix make_norn_step_runner session persistence",
      "spec": "WHEN make_norn_step_runner builds an AgentBuilder for a workflow step, THE SYSTEM SHALL call .session(store) with a JsonlSink-backed EventStore. The session file SHALL be stored at {data_dir}/sessions/workflow/{execution_id}_{step_number}.jsonl. THE SYSTEM SHALL NOT use bare EventStore::new() for workflow steps.",
      "acceptance": [
        "AgentBuilder::session(store) is called with a JsonlSink-backed EventStore",
        "Session file path uses execution_id and step_number",
        "Workflow session files are at {data_dir}/sessions/workflow/",
        "Events persist to disk during workflow step execution"
      ],
      "files": {
        "modify": ["crates/meridian-services/src/workflow/imperative_callbacks.rs"]
      },
      "checklist": ["C40", "C41"],
      "stories": ["S21"]
    },
    {
      "id": "R2",
      "title": "WebSocket compatibility verification",
      "spec": "The system SHALL verify that ws/assistant.rs, ws/session_replay.rs, and try_resubscribe work unchanged with Norn-powered sessions. No modifications to these files. Verification is by inspection and integration test.",
      "acceptance": [
        "ws/assistant.rs has no modifications from this cluster",
        "ws/session_replay.rs has no modifications from this cluster",
        "WS handler subscribes to broadcast::Sender<AssistantEvent> from a Norn session",
        "try_resubscribe works when Norn broadcast sender drops and the session re-wakes"
      ],
      "files": {},
      "checklist": ["C66", "C67", "C68"],
      "stories": ["S17"]
    },
    {
      "id": "R3",
      "title": "Clippy and existing test preservation",
      "spec": "cargo clippy --workspace -- -D warnings SHALL pass clean after all briefs land. All existing assistant service tests SHALL pass without modification.",
      "acceptance": [
        "cargo clippy --workspace -- -D warnings exits 0",
        "All tests in service.rs, session_loop.rs, translate.rs, events.rs pass unchanged",
        "No existing test is modified or deleted"
      ],
      "files": {},
      "checklist": ["C69", "C70"],
      "stories": []
    },
    {
      "id": "R4",
      "title": "Norn session loop integration tests",
      "spec": "The system SHALL include tests verifying the complete event translation pipeline: a mock Norn agent emitting ProviderEvents → NornTranslateState producing AssistantEvents → broadcast channel delivery. Tests SHALL verify TextDelta, ToolUseStarted, ToolResult, TurnComplete, and SubAgentEvent lifecycle.",
      "acceptance": [
        "Tests in norn_translate.rs cover all ProviderEvent → AssistantEvent mappings",
        "Tests verify SessionStarted synthesis before first event",
        "Tests verify sub-agent wrapping for child agent_ids",
        "Tests verify turn boundary state clearing"
      ],
      "files": {
        "modify": [
          "crates/meridian-services/src/assistant/norn_translate.rs",
          "crates/meridian-services/src/assistant/norn_session.rs"
        ]
      },
      "checklist": ["C71"],
      "stories": []
    },
    {
      "id": "R5",
      "title": "Namespace tool tests",
      "spec": "Each namespace tool SHALL have tests verifying all commands with mocked service layer. Tests SHALL verify input_schema correctness, command dispatch, authorization scoping to CallerContext.member_id, and structured JSON output.",
      "acceptance": [
        "MessagingTool tests cover all 12 commands",
        "MemberTool tests cover all 7 commands",
        "WorkspaceTool tests cover all 4 commands",
        "Tests use mock ServiceContext",
        "Tests verify CallerContext authorization scoping"
      ],
      "files": {
        "modify": [
          "crates/meridian-tools/src/messaging.rs",
          "crates/meridian-tools/src/member.rs",
          "crates/meridian-tools/src/workspace.rs"
        ]
      },
      "checklist": ["C72"],
      "stories": []
    },
    {
      "id": "R6",
      "title": "Session persistence tests",
      "spec": "The system SHALL include tests verifying NornSessionStore create, resume, and fork flows. Tests SHALL verify JSONL file creation, event replay on resume, file copy on fork, and missing file fallback.",
      "acceptance": [
        "Test: new session creates JSONL file at expected path",
        "Test: resume reads existing JSONL and pre-populates EventStore",
        "Test: fork copies source file and builds from copy",
        "Test: missing file on resume falls back to new session",
        "Tests use tempdir for file operations"
      ],
      "files": {
        "modify": ["crates/meridian-services/src/assistant/norn_session_store.rs"]
      },
      "checklist": ["C73"],
      "stories": ["S19"]
    },
    {
      "id": "R7",
      "title": "Line count compliance",
      "spec": "No file created or modified by this cluster SHALL exceed 500 lines of code (excluding tests, comments, and whitespace). WHEN a file approaches the limit, THE SYSTEM SHALL split it into focused sub-modules.",
      "acceptance": [
        "tokei reports ≤500 code lines for every file in the cluster",
        "norn_session.rs, norn_translate.rs, and each namespace tool file are under 500 lines"
      ],
      "files": {},
      "checklist": ["C74"],
      "stories": []
    }
  ],
  "dependency_setup": "NR-008 test requirements for meridian-tools dev-deps (established by NR-006): tempfile.workspace=true, tokio={workspace=true, features=[\"macros\",\"rt-multi-thread\",\"test-util\"]}. No other new dependencies needed — meridian-services already has all required dev-deps.",
  "boundaries": [
    "SHALL NOT modify ws/assistant.rs or ws/session_replay.rs",
    "SHALL NOT modify existing tests — only add new ones",
    "SHALL NOT modify the norn crate",
    "SHALL NOT exceed 500 lines of code per file"
  ],
  "verification": [
    "cargo clippy --workspace -- -D warnings passes",
    "cargo test --workspace passes all existing and new tests",
    "tokei confirms all files under 500 code lines",
    "make_norn_step_runner creates JSONL-backed EventStore",
    "WebSocket handler works with Norn sessions (integration test or manual verification)"
  ]
}
