---
type: brief
id: NUI-002
cluster: norn-retained-tui
title: Use the reserved tool envelope description in retained tool rows
---

# NUI-002: Use the reserved tool envelope description in retained tool rows

> **Cluster:** norn-retained-tui
> **Depends on:** NUI-001
> **Checklist:**
> - C5 — Compact known/unknown calls and honest per-call Changes
> **Stories:**
> - S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.

## Purpose

Repair every-tool description unavailable caused by reading ordinary description rather than the reserved envelope field.

## Task

Correct live and restored retained tool descriptions, validate and install the next local preview while preserving appearance and changed-cell rendering.

## Requirements

### R1: Correct shared description extraction

Use crate::tool::ENVELOPE_DESCRIPTION_KEY for function-call metadata in live/native completion, completed Responses items, modern replay and legacy tool_calls replay. Never read ordinary description as envelope metadata.

**Acceptance:**
- Use crate::tool::ENVELOPE_DESCRIPTION_KEY for function-call metadata in live/native completion, completed Responses items, modern replay and legacy tool_calls replay. Never read ordinary description as envelope metadata.
- One borrowed Value extractor serves parsed modern/live arguments and already-parsed legacy values. Do not invoke split_envelope_fields: projection does not coerce or log invalid user values.
- Absent reserved key yields absent description/no error; empty or whitespace-only string yields absent description and named empty diagnostic; present non-string, null or non-object arguments yield absent description and named shape diagnostic with no payload values. A nonblank string retains its original content through existing DisplayText safety transformation.
- Freeform Custom calls preserve their existing no-extraction behavior for JSON-looking and invalid-JSON inputs. No argument-body changes, model changes or runtime settings changes.

**Files:**
- modify: crates/norn/src/session_view/tools.rs
- modify: crates/norn/src/session_view/committed.rs

**Checklist:**
- C5 — Compact known/unknown calls and honest per-call Changes

**Stories:**
- S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.

### R2: Prove live and historical extraction

Correct the three existing fixtures incorrectly calling envelope prose description, preserving their identity/join assertions.

**Acceptance:**
- Correct the three existing fixtures incorrectly calling envelope prose description, preserving their identity/join assertions.
- Exercise live ToolCallComplete, live ResponseItemDone, modern committed response_items and legacy tool_calls using both fields with different content. Ordinary-description-only input must not become a compact description.
- Test absent/empty/whitespace/non-string/null/non-object cases, exact original nonblank text, diagnostic payload privacy, invalid serialized function JSON and custom payload non-extraction. Preserve existing original assertions.

**Files:**
- modify: crates/norn/src/session_view/projection_tests.rs

**Checklist:**
- C5 — Compact known/unknown calls and honest per-call Changes

**Stories:**
- S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.

### R3: Publish a checked local repair checkpoint

Validate and install the native repair while retaining preview.2 rollback bytes.

**Acceptance:**
- Change only the four workspace package versions; no dependency upgrades.
- Run semantic projection tests, retained TUI checks, formatting, strict workspace/all-target Clippy and actual AST policy before local installation.
- Preserve source-bound receipts, atomic installation with rollback, version/hash verification and the requested Mac speech. Main landing remains pending exact205 and review.

**Files:**
- modify: Cargo.toml
- modify: Cargo.lock
- modify: docs/release-notes/UNRELEASED.md

**Checklist:**
- C5 — Compact known/unknown calls and honest per-call Changes

**Stories:**
- S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.

## Verification

- Agent scratch preparation used Rust 1.94 formatting, actual AST scans, patch checks and preimage hashes; it did not run Cargo. Root verified the three preimages against the UI worktree before application.
- The earlier preferences test build was interrupted; it contributes no test verdict to this repair.
- Final identical-source local checks passed: 44 session-view tests (/private/tmp/ntc-001-proof/projection-tests-03.json), 809 TUI unit tests (tui-tests-02.json), formatting (fmt-04.json), strict workspace/all-target Clippy (clippy-03.json), actual AST policy (policy-06/ast-policy.json) and the optimized build (release-build-01.json). All relative receipt names in this sentence are under /private/tmp/ntc-001-proof/.
- Installed norn 0.1.0-preview.3 on 6 September 2026 at 12:15:10 Melbourne from 264700821b8df07226b0073053297857207b1809. Receipt: /Users/tom/.local/state/norn/install-backups/20260906-121510-description-preview/receipt.json. /private/tmp/ntc-001-proof/installation-confirmed.json records successful Mac speech. The prior preview.2 bytes remain beside the receipt for rollback.
- Local preview only. The final checks and build bind identical source. No exact205 battery, Fable review, main landing, complete workspace test suite, new PTY run or public release is claimed for this repair.
