---
type: brief
id: NRT-001
cluster: norn-retained-tui
title: Retained foreground transcript and read-only tool Changes
---

# NRT-001: Retained foreground transcript and read-only tool Changes

> **Cluster:** norn-retained-tui
> **Depends on:** NCS-001
> **Checklist:**
> - C0 — Register the settled checkpoint on frozen NCS
> - C1 — Terminal-independent identities and deterministic projection
> - C2 — Demanded history and approved body ranges
> - C3 — Migrate every transcript and status producer
> - C4 — Own frame, layout, caret and styled content
> - C5 — Compact known/unknown calls and honest per-call Changes
> - C6 — Reading, selection/copy, search/export and existing controls
> - C7 — Real PTY checkpoint and exact-commit evidence
> - C8 — R4.layout — independent pure geometry foundation
> **Stories:**
> - S1 (Session participant, human operator or observing model) — As a session participant I can distinguish committed, provisional and incomplete history, and inspect the exact recorded body without affecting execution.
> - S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.
> - S3 (Session participant, human operator or observing model) — As a keyboard or mouse reader I can find, select, copy and export content and retain all current controls, with real terminal proof.

## Purpose

Make terminal history compact, readable and stable under resize while preserving exact semantic evidence and existing execution behavior.

## Task

Implement the settled NRT-001 checkpoint on frozen NCS commit f0cb7476ceb80ce6b9c85a02088cdb8297e960a3. R0 registers this contract; R1, R8 (R4.layout) and R9 (R4.text) may then run independently. R2 retains semantic completion dependency on R1, with early dispatch after the fixed shared contract under D-R2-DISPATCH; R3 follows R2; R4 follows R3, R8 and R9; R5, R6 and R7 follow sequentially. Root owns R8 and R9; one integration owner owns overlapping R3-R6 paths. NCS must be exact-commit venue-green and integrated before NRT landing. If NCS changes, reconcile/rebase and repeat exact-candidate verification. No approval or preference placeholder remains. Stop and name required files before editing outside a row wall.

## Requirements

### R0: Register the settled checkpoint on frozen NCS

WHEN this settled checkpoint is registered, THE SYSTEM SHALL record the source baseline, decisions, local producer/acceptance contracts, checklist, stories and exact walls before implementation. Registration authorizes stacked implementation without treating pending NCS verification as green.

**Acceptance:**
- F1..F6 are recorded as settled decisions with explicit defaults and action semantics; no conditional wall or preference placeholder remains.
- Existing estate design/cluster renderers reproduce Markdown from JSON; existing coverage checker reports every checklist/story assigned. Producer/acceptance references resolve to files in this cluster.
- The refreshed inventory is bound to f0cb7476ceb80ce6b9c85a02088cdb8297e960a3 with the exact51-path NCS delta and unchanged TUI/event contracts; root owns external programme/tracking updates. NCS and NRT landing remain separately verified.
- Design provenance: D1-D6, F1-F6 in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.

**Files:**
- create: docs/design/norn-retained-tui/design.json
- create: docs/design/norn-retained-tui/DESIGN.md
- create: docs/design/norn-retained-tui/checklist.json
- create: docs/design/norn-retained-tui/CHECKLIST.md
- create: docs/design/norn-retained-tui/stories.json
- create: docs/design/norn-retained-tui/USER-STORIES.md
- create: docs/design/norn-retained-tui/briefs/NRT-001.json
- create: docs/design/norn-retained-tui/briefs/NRT-001.md
- create: docs/design/norn-retained-tui/producer-coverage.json
- create: docs/design/norn-retained-tui/acceptance.json

**Checklist:**
- C0 — Register the settled checkpoint on frozen NCS

**Stories:**
- S1 (Session participant, human operator or observing model) — As a session participant I can distinguish committed, provisional and incomplete history, and inspect the exact recorded body without affecting execution.

### R1: Terminal-independent identities and deterministic projection

WHEN a typed live or committed record arrives, THE SYSTEM SHALL reduce it through norn::session_view using I1..I8, without terminal types, disk I/O, provider calls or a duplicate host reducer.

**Acceptance:**
- Test same-name overlapping tools, item_id→call_id aliases, freeform calls, orphan results and duplicates across page boundaries.
- Test retry within a multi-response turn, authoritative response_items versus flat projections, refusal, cancellation and provisional alias invalidation; never identify by text equality.
- Explicit body allowlist and unknown custom-event classification exclude opaque/private provider replay state. Each current enum variant has a disposition fixture; future variants force exhaustive review.
- No Ready/snapshot subscription API or durable generation claim is introduced.
- Implement the six public core seams named in design.json.public_core_contracts. Cursor/body minting is crate-internal and source-owner validated; public consumers cannot construct arbitrary approved roots/fields.
- Projection inputs/history items carry compact approved metadata and body references, never cloned raw SessionEvent/ResponseTranscriptItem payloads containing opaque/private state. Validate every provenance/correlation boundary and retain unknown origin explicitly.
- Design provenance: I1-I8, D4, producer-coverage.json in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.
- Each committed insertion/page application uses incremental ordering and direct identity/per-call lookup; it must not sort/join the entire view or rescan full history for each orphan. Meaningful operation-count fixtures distinguish local updates from total-history work; no invented timing SLA.
- A result whose parent invocation is outside the loaded page stays explicitly pending/unresolved until the correct invocation is available. Reuse of a call_id in a different invocation/response scope must not silently attach that result to the wrong call.
- ToolItem → ToolCall → Committed alias chains preserve the original early streaming anchor through final commit. Alias resolution is transitive, cycle-safe and source-scoped; index updates do not drop prior aliases.
- The ordered index stores each ViewItem once, exposes ordered iterator/range/lookup access and shares existing body references; no second body store or duplicated full retained item collection.
- Expose a borrowed indexed traversal beginning at an exact current ItemId in either direction, with declared inclusion semantics, using the existing ID-to-order lookup and BTreeMap range. Pinned viewport painting must not linearly seek through all earlier/later compact items. Missing or foreign identity is explicit, never an empty success or unrelated fallback; proven alias resolution remains separately explicit. No body loading/cloning, extra row owner or provider work. Regression fixtures cover committed/live boundaries, inclusivity, missing/foreign IDs and work bounded by requested rows rather than retained history length.

**Files:**
- create: crates/norn/src/session_view/mod.rs
- create: crates/norn/src/session_view/contract.rs
- create: crates/norn/src/session_view/projection.rs
- create: crates/norn/src/session_view/body.rs
- create: crates/norn/src/session_view/error.rs
- create: crates/norn/src/session_view/contract_tests.rs
- create: crates/norn/src/session_view/projection_tests.rs
- create: crates/norn/src/session_view/live.rs
- create: crates/norn/src/session_view/committed.rs
- create: crates/norn/src/session_view/response.rs
- create: crates/norn/src/session_view/tools.rs
- create: crates/norn/src/session_view/index.rs
- create: crates/norn/src/session_view/local.rs
- modify: crates/norn/src/lib.rs

**Checklist:**
- C1 — Terminal-independent identities and deterministic projection

**Stories:**
- S1 (Session participant, human operator or observing model) — As a session participant I can distinguish committed, provisional and incomplete history, and inspect the exact recorded body without affecting execution.

### R2: Demanded history and approved body ranges

WHEN the view requests history or a body, THE SYSTEM SHALL validate the source/cursor/reference/range and return only the requested data or an explicit typed failure, without holding store locks during filesystem work.

**Acceptance:**
- Refuse wrong source/generation, mismatched EventId/ordinal, stale provisional revision, forged/traversing/outside-root spool references and unavailable/malformed bodies.
- Real temporary spool tests prove requested byte ranges, UTF-8 chunk reconstruction, short reads and file failures; raw JSON-range reads do not claim decoded arbitrary field paging.
- R2 may transiently clone only the explicitly selected SessionEvents while holding the store lock, release the lock, and call project_committed off-lock. This bounded selected-event staging is not retained view state. Every public/retained HistoryPage carries only compact HistoryRecord values and approved body capabilities, never raw SessionEvent/opaque provider payloads. No filesystem I/O or display projection occurs under the store lock; no full-history clone is introduced. Render/resize have zero history/body reads; explicit read actions retain demand and coverage state.
- No new committed push/append-family edits: later host owner must integrate ordinary, batch, idempotent and provider-identity-adoption paths separately. Do not emit a fake durable cursor.
- Initialize a distinct local store_generation UUID in all three EventStore constructors and validate every cursor against that instance/session binding. Empty-start is explicit; no durable generation is claimed.
- Range reads use the owning SpoolWriter private data_dir/registered/root_session_id and PrivateRoot; no caller-supplied root path or exported arbitrary-path capability.
- Design provenance: I6-I8, F1, F4, session-host-review STORE-COMMIT/BODY-RANGES in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.
- Dispatch provenance D-R2-DISPATCH: early disjoint implementation is authorized after shared contract freeze; integrated acceptance requires R1 and R2 completed.
- Modify crates/norn/src/session/branch.rs only to add a pub(crate) MailboxId::generation() -> Uuid accessor, or equivalent crate-private equality check. Initial managed EventStore view-source binding must validate actual SessionBinding/session-index generation against its owning SpoolWriter registered.generation as well as the session ID. Preserve all other branch behavior, existing mailbox serde/wire representation and visibility outside the crate. No generic identity service is introduced.
- R2-01 preserves exact ToolResult/spool_ref bytes and response-publication commitments. Closed inherited-spool authority is a publication-owned sidecar at the fixed owner-derived <destination-root>/spool-inheritance.json path, never an appendable Custom or ChildBranch grant. The existing atomic independent-fork journal carries its exact closed manifest: destination session/index generation, actual source session/generation/path, actual ChildBranch EventId/anchor, and unique exact copied EventId/reference grants with validated origin generations. Recovery publishes deterministic manifest bytes through a private temporary and exclusive publication before timeline/index publication; retries verify an existing sidecar exactly. Preserve existing journal recovery and strict directory inventory, admitting only the journal-declared known sidecar alongside audio. Resume/read hydration refuses duplicate, conflicting or mismatched manifests; source and destination registered generations are checked in one index transaction before PrivateRoot range reads. Carry only publication-owned validated prior grants through fork-of-fork. Appending a forged well-shaped Custom and matching ChildBranch with real current generation values confers zero authority. Pre-feature cross-root references without durable source-generation proof remain explicitly unavailable. R2-02 revalidates the current attached SpoolWriter against the immutable owner binding before every history/body read. No Custom metadata event, new index field, signature service, whole-body spool decoding or raw provider-state display is introduced.
- Real tests exercise independent fork -> demanded body read -> resume -> read -> fork-of-fork, source deletion/recreation, malformed authority, and writer replacement after a capability was minted. Generation metadata stays non-display and no raw scalar values enter malformed-authority errors. Additional persistence/append paths require a further explicit wall amendment before edits.
- Exercise journal stops before/after sidecar publication and before/after timeline/index publication, exact idempotent recovery, malformed/conflicting sidecars, and combined audio+spool known-file inventories. Preserve existing journal-version behavior for prior timeline/audio transactions. Source generation changes cannot substitute new bytes behind an inherited capability.
- Core test-only cleanup within current modified source walls: review_child_context exclusively owns loop/commands.rs, session/branch.rs and session/spool.rs under crates/norn/src. norn_models retains loop/classify.rs and provider/agent_event.rs. Remove the five inherited allow attributes and resolve their underlying test unwrap/expect uses, preserving every semantic assertion and production bytes for this cleanup. Propagate Result failures; no skips, weaker assertions, wildcard bypasses or unrelated unchanged-module changes. Common-owner Clippy style repairs are a separately identified existing task, not permission to change production through this test-cleanup partition. The 52-file pre-thaw core AST snapshot remains historical evidence. Source ownership must be acknowledged before parallel cleanup; no concurrent writes to a shared file. The whole-workspace Clippy01 run has ended and root explicitly released the document freeze. These are existing paths only and all NRT acceptance and final verification requirements remain.
- The existing modified-core test cleanup includes inherited panic/panic!, expect_err and unwrap_err as well as unwrap and expect. Replace these with contextful fallible Result diagnostics while preserving every original semantic assertion and production bytes for the cleanup. No new skips, weaker assertions, or replacement bypasses.

**Files:**
- create: crates/norn/src/session/store/history_page.rs
- create: crates/norn/src/session/store/history_page_tests.rs
- create: crates/norn/src/session/spool/range.rs
- create: crates/norn/src/session/spool/range_tests.rs
- create: crates/norn/src/session/spool/inheritance.rs
- create: crates/norn/src/session/spool/inheritance_tests.rs
- create: crates/norn/src/session/persistence/publication_spool.rs
- modify: crates/norn/src/session/store.rs
- modify: crates/norn/src/session/spool.rs
- modify: crates/norn/src/session/branch.rs
- modify: crates/norn/src/session/manager/fork.rs
- modify: crates/norn/src/session/manager/open.rs
- modify: crates/norn/src/session/persistence/index.rs
- modify: crates/norn/src/session/persistence/publication.rs
- modify: crates/norn/src/session/persistence/publication_journal.rs
- modify: crates/norn/src/session/persistence/publication_audio.rs
- modify: crates/norn/src/session/persistence/publication_tests.rs

**Checklist:**
- C2 — Demanded history and approved body ranges

**Stories:**
- S1 (Session participant, human operator or observing model) — As a session participant I can distinguish committed, provisional and incomplete history, and inspect the exact recorded body without affecting execution.

### R3: Migrate every transcript and status producer

WHEN any current TUI producer emits user-visible content or current state, THE SYSTEM SHALL retain it semantically through one local projection adapter and preserve its origin and execution semantics.

**Acceptance:**
- All entries in producer-coverage.json have executable assertions for semantic output/state, including child-result batches, steered/queued delivery, root inbound wake, MCP command completion/refusal, all step outcomes and startup/new/compaction notices.
- Broadcast lag creates a visible incomplete state and boundary reconciliation without repeating tools or inventing missing transient events; forced lag case is a required fixture.
- /new swaps store binding and view generation coherently with existing rotation dependencies; historical data and draft treatment follow D-F3/D-F5 controls. Resume loads demanded history with truthful older-coverage status.
- Channels remain external framed content; no slash parsing or fabricated human sender. Child results retain actual child/source identity even where the old display used write_user_message.
- All production writes leave semantic producers; only the later single frame/terminal owner writes terminal bytes. This row and R4 are an integration unit, not separately shippable UI.
- ViewConfig owns the declared history/body/clipboard preferences from D-F4, with typed validation, visible effective values and explicit /view updates. R1/R2 contain no default demand sizes. Geometry defaults remain solely in render/layout.rs, avoiding duplicated constants.
- Design provenance: D4-D5, I1-I8, producer-coverage.json in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.
- Pass an explicit Arc<SessionBinding> into TuiInputs, whose declaration is in app/event_loop.rs already inside R3. The real CLI obtains its actual AgentToolInfra.session from its assembled coordination runtime; missing required infra is an explicit launch error, never a fabricated identity. Initialize and validate ViewSource through EventStore::bind_view_source. /new validates the newly created binding before the existing rotation commit, then replaces the view source and cursor/body state. Add transparent ViewError and HistoryReadError to TuiError in error.rs. Update only binding/error wiring in the three existing TuiInputs fixtures using their actual ephemeral setup; their semantic/PTY assertions remain unchanged until the sequential R7 work. Preserve resolved NCS launch-mode validation before provider/MCP construction.
- Update AppState::new fixtures in app/autocomplete.rs and app/edit.rs only to supply explicit isolated ViewSource values. Production AppState requires an owner-supplied validated source; no unbound Option or invented disk authority. This fixture-only wall addition preserves their existing behavior assertions.
- R3/R5 completion requires the NRT-002 exact producer publication-binding repair and its required proof. Existing implementation/integration authoring may continue under exclusive coordinated ownership; duplication, guessed association or an unverified repair cannot be reported complete. NRT-001 and NRT-002 form one final checkpoint; completion gates do not add circular source-authoring prerequisites.
- For one exact admitted execution, normal End and TurnCompleted facts share one compact completed-execution presentation row. Selection or expansion exposes every original completion fact, source, model, usage, timing and availability field. Errors, cancellation and coverage gaps remain explicit. Never drop metadata, invent success or associate records by text, time or proximity. Implement presentation only through existing TUI file walls.
- Pure parallel authoring slice: render/retained_structured.rs and retained_structured_tests.rs provide a source-mapped RenderedMarkdown adapter for complete multi-field assistant JSON, preserving the existing secondary-fields/Ctrl+E acceptance. Every displayed field/range maps back to the supplied approved original source; partial JSON remains the original text with explicit partial state. Do not synthesize BodyRefs, invent content/fields or parse ANSI. No module wiring, body I/O, provider work or runtime mutation in this slice; the TUI owner integrates it against current source/revision mapping and retains all original bytes and secondary fields.
- Tom's no-allow-anywhere instruction overrides the inherited CLAUDE.md test exception. Before landing, remove inherited allow attributes and resolve the underlying test unwrap/expect uses in the eleven already-modified TUI files named by this partition. Preserve every assertion and propagate failures through Result; no skips, weaker assertions, renamed unused values or replacement lint bypasses. This is test-only cleanup of these current modified files, not unrelated unchanged-module or public-history audit work. Exclusive test-cleanup partition: review_child_context owns test-only cleanup in crates/norn-tui/src/agents/tabs.rs, crates/norn-tui/src/app/autocomplete.rs, crates/norn-tui/src/app/helpers.rs, crates/norn-tui/src/events/schema_render.rs, crates/norn-tui/src/render/fixed_panel.rs, crates/norn-tui/src/render/markdown.rs, crates/norn-tui/src/render/text.rs. norn_tui retains crates/norn-tui/src/app/dispatch.rs, crates/norn-tui/src/app/event_loop.rs, crates/norn-tui/src/app/slash.rs, crates/norn-tui/src/app/state.rs. The active source owner must explicitly acknowledge handoff before parallel cleanup starts; no concurrent writes to any shared file. Production behavior remains unchanged, and all current brief acceptance and final verification requirements remain.
- Root records the TUI owner's explicit handoff of test-only crates/norn-cli/src/tui/driver.rs to review_child_context. This existing NRT-001 R3 path has one inherited allow attribute and one test expect operation. Remove those structurally with a contextful fallible Result while preserving the real pre-terminal startup/error assertion and all test attributes. The production prefix must remain byte-identical to the final core/CLI audit; no production behavior change, suppression, skip or weaker oracle.

**Files:**
- create: crates/norn-tui/src/app/transcript.rs
- create: crates/norn-tui/src/app/transcript_tests.rs
- create: crates/norn-tui/src/app/notices.rs
- create: crates/norn-tui/src/app/view_config.rs
- create: crates/norn-tui/src/app/view_config_tests.rs
- create: crates/norn-tui/src/render/retained_structured.rs
- create: crates/norn-tui/src/render/retained_structured_tests.rs
- modify: crates/norn-tui/src/app/mod.rs
- modify: crates/norn-tui/src/app/state.rs
- modify: crates/norn-tui/src/app/dispatch.rs
- modify: crates/norn-tui/src/app/dispatch/finalization.rs
- modify: crates/norn-tui/src/app/streaming.rs
- modify: crates/norn-tui/src/app/tool_calls.rs
- modify: crates/norn-tui/src/app/session_replay.rs
- modify: crates/norn-tui/src/app/slash.rs
- modify: crates/norn-tui/src/app/model_selection.rs
- modify: crates/norn-tui/src/app/mcp_slash.rs
- modify: crates/norn-tui/src/app/turn/run.rs
- modify: crates/norn-tui/src/app/turn/mid.rs
- modify: crates/norn-tui/src/app/rotation.rs
- modify: crates/norn-tui/src/app/child_results.rs
- modify: crates/norn-tui/src/app/helpers.rs
- modify: crates/norn-tui/src/app/event_loop.rs
- modify: crates/norn-tui/src/app/render.rs
- modify: crates/norn-tui/src/agents/tabs.rs
- modify: crates/norn-tui/src/events/schema_render.rs
- modify: crates/norn-cli/src/tui/driver.rs
- modify: crates/norn-tui/src/error.rs
- modify: crates/norn-tui/tests/pty_smoke.rs
- modify: crates/norn-tui/tests/model_selection.rs
- modify: crates/norn-tui/tests/support/mcp_channels_tui.rs
- modify: crates/norn-tui/src/app/autocomplete.rs
- modify: crates/norn-tui/src/app/edit.rs

**Checklist:**
- C3 — Migrate every transcript and status producer

**Stories:**
- S1 (Session participant, human operator or observing model) — As a session participant I can distinguish committed, provisional and incomplete history, and inspect the exact recorded body without affecting execution.

### R4: Own frame, layout, caret and styled content

WHEN the app paints or receives geometry/input, THE SYSTEM SHALL compose one full-screen frame under the viewport invariants and restore terminal modes on every supported exit path.

**Acceptance:**
- Alternate-screen entry/leave, bracketed paste, mouse/Kitty/synchronized output and cursor restoration are owned exactly once. No production DECSTBM scroll-growth/cursor-accounting API remains.
- Retain semantic markdown/code/diff/structured formatting as direct styled rows; no ANSI capture/parser/terminal emulator is introduced. Renderer errors are handled visibly. Permit a direct unicode-segmentation = "1.13.2" dependency in crates/norn-tui/Cargo.toml and only its norn-tui dependency-list membership in Cargo.lock. The exact version already exists in the lockfile; no package version/checksum churn or other dependency/policy change. Grapheme boundaries govern retained styled-row clipping, wrapping and selection; scalar-only clipping is not an acceptable substitute. Existing ANSI-producing APIs are adapted to direct styled spans; no ANSI parser/capture is introduced.
- Full-width composer, no global header/footer; narrow switch/restore, popup and focus follow the D-F2 geometry. Zero/tiny sizes preserve state without coordinate underflow or accidental submission.
- Resize invalidates layout only; stable logical anchor/selection, draft, provider-call count and store-event count remain unchanged. Incoming live updates cannot steal focus.
- Update crates/norn-tui/Cargo.toml package description from obsolete DECSTBM wording and add only the dependency authorized by D-R4-GRAPHEMES. R8 owns layout.rs/layout_tests.rs; integration consumes its public rectangles/geometry without editing those files concurrently.
- Design provenance: D1, viewport_invariants, WS-01, WS-02, WS-16 in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.
- The retained Markdown adapter uses the existing pulldown-cmark dependency and emits R9 StyledText directly, with no ANSI intermediates. It retains source/display spans for exact text and explicit transformed or generated segments. Controls are escaped before display offsets are recorded; entities, escapes, link syntax and generated list/break markers never pretend to have one-to-one byte offsets. Fixed examples cover nested inline/block formatting, code, lists, tables, links, HTML-as-text and streamed incomplete syntax. The frame must consume its declared mapping for original-byte anchors/copy; incomplete chunks do not silently claim complete Markdown formatting.
- Keep render responsibilities within the 500 production-line limit: app/render.rs coordinates ScreenState/frame publication; app/render/transcript.rs prepares visible transcript/body mapping; app/render/composer.rs prepares the draft and completion popup. retained_markdown/mapping.rs owns the original/display mapping types and source-boundary queries, re-exported without API changes by retained_markdown.rs. These named modules add no separate renderer owner or fallback behavior.
- agents/mod.rs changes are re-export-only: remove obsolete native-scrollback replay helper exports while preserving TabState and retained-session exports. No implementation belongs in mod.rs.
- Tom's no-allow-anywhere instruction overrides the inherited CLAUDE.md test exception. Before landing, remove inherited allow attributes and resolve the underlying test unwrap/expect uses in the eleven already-modified TUI files named by this partition. Preserve every assertion and propagate failures through Result; no skips, weaker assertions, renamed unused values or replacement lint bypasses. This is test-only cleanup of these current modified files, not unrelated unchanged-module or public-history audit work. Exclusive test-cleanup partition: review_child_context owns test-only cleanup in crates/norn-tui/src/agents/tabs.rs, crates/norn-tui/src/app/autocomplete.rs, crates/norn-tui/src/app/helpers.rs, crates/norn-tui/src/events/schema_render.rs, crates/norn-tui/src/render/fixed_panel.rs, crates/norn-tui/src/render/markdown.rs, crates/norn-tui/src/render/text.rs. norn_tui retains crates/norn-tui/src/app/dispatch.rs, crates/norn-tui/src/app/event_loop.rs, crates/norn-tui/src/app/slash.rs, crates/norn-tui/src/app/state.rs. The active source owner must explicitly acknowledge handoff before parallel cleanup starts; no concurrent writes to any shared file. Production behavior remains unchanged, and all current brief acceptance and final verification requirements remain.
- review_tui exclusively owns the approved legacy syntax repair in render/syntax.rs, render/content.rs, render/markdown/emitter.rs, render/markdown.rs, events/schema_render.rs and tools/rich/edit.rs under crates/norn-tui/src. The last file changes test callers only. Coordinate the already modified Markdown/schema files with their frozen owners before writes; no retained App or unrelated tool-renderer wiring is authorized.
- Change legacy SyntaxHighlighter::highlight to Result<String, SyntaxError>, retaining the original syntect::Error as typed source and the actual code-line byte offset. Never turn highlighting failure into an empty range list, omit an original line or return a labelled successful String. Replace the four discarded format Results with infallible String construction. The already typed retained highlight_spans contract is unchanged.
- Propagate the same typed SyntaxError through legacy render_blocks/code/diff, emitter handle/end-code, MarkdownRenderer render_segment/try_flush_styled/feed/finalize and schema render_assistant_message/render_structured/render_event/field helpers. Existing infallible user/thinking/tool/status arms remain successful values. Adapt all exact legacy test callers fallibly; no unwrap, expect, ignore or replacement suppression.
- Render the candidate Markdown segment before draining pending source or publishing paragraph/dim state. A failure retains original pending content and consistent state; do not duplicate or replay an already accepted feed chunk. Borrowed ContentBlock/SessionEvent/JSON source bodies remain unchanged. No partial ANSI output may escape as a successful render after failure.
- Prove a deterministic highlighting error retains its syntect source and exact byte offset, and prove failed streaming rendering preserves pending source/state. Preserve every existing ANSI/formatting expectation, successful multiline and numbered code, both diff sides and complete/partial fence behavior. Current retained App uses a separate typed path; this repair proves no new live-App recovery or on-screen error presentation.
- Keep the existing no-allow-anywhere instruction: newly touched test modules cannot add or rely on inherited suppression to permit new fallible-call bypasses. Any complete test-only cleanup extent is explicitly assigned separately. Required tests, formatting, strict workspace Clippy and source policy remain pending until actual execution; this registration is not a pass.
- Root additionally assigns review_tui complete test-only policy cleanup in these two already-approved syntax-repair files: remove the inherited allow attribute in render/content.rs (unwrap_used, missing_const_for_fn, uninlined_format_args) and resolve its underlying test style structurally; remove the inherited unwrap_used allow plus all six test unwraps in tools/rich/edit.rs. Preserve every test attribute, assertion predicate and meaningful refusal through contextful fallible Result diagnostics. tools/rich/edit.rs production bytes remain identical; content.rs production changes are limited to the separately approved typed syntax propagation. No unrelated production changes, replacement suppression, skip or weaker oracle. This cleanup remains a release/landing requirement and is not grounds to mislabel a separate ready preview as a release.
- Direct user correction at 09:16 Melbourne on 6 September 2026: restore the original visual styling inside the retained fullscreen renderer; do not roll back, discard current work or return to the old terminal ownership. Deliver the local fix within 30 minutes (requested 09:46 Melbourne). norn_tui owns exactly render/frame.rs, render/layout.rs, render/layout_tests.rs, app/render.rs, app/render/composer.rs and app/render/transcript.rs under crates/norn-tui/src. Restore the earlier dim top-left mode/token rule, plain input, bottom-right model/effort/tier/session rule and last-row key hints, keeping the composer full width. Reuse the inspected main styling; the owner's stated three chrome rows yield when terminal height is below six. This direct user ruling supersedes earlier no-footer/minimal-chrome appearance clauses only, not retained-frame ownership, resize safety or source identity.
- Restore the original blue > first-line user prompt, two-column continuations and blank turn margin; remove generic You submitted/Assistant presentation headings. Preserve semantic role, exact source maps, IDs, original body bytes, keyboard operation and full-width composition. Borders, colors, prompt labels and layout are presentation changes; never reconstruct/drop transcript evidence or change runtime input meaning.
- The ordinary TUI view hides only the typed Metadata category and exposes it through explicit detail inspection; do not remove it from the semantic projection or persisted history, or use arbitrary text/label matching. norn_tui owns app/render/transcript.rs presentation integration; root coordinates any shared visibility wiring with that owner rather than writing the same file concurrently. Existing data preservation, publication binding, completion grouping, pinned reading and typed error requirements remain.

**Files:**
- create: crates/norn-tui/src/render/frame.rs
- create: crates/norn-tui/src/render/frame_tests.rs
- create: crates/norn-tui/src/app/viewport.rs
- create: crates/norn-tui/src/app/viewport_tests.rs
- create: crates/norn-tui/src/app/focus.rs
- create: crates/norn-tui/src/render/retained_markdown.rs
- create: crates/norn-tui/src/render/retained_markdown_tests.rs
- create: crates/norn-tui/src/app/render/transcript.rs
- create: crates/norn-tui/src/app/render/composer.rs
- create: crates/norn-tui/src/render/retained_markdown/mapping.rs
- modify: crates/norn-tui/src/lib.rs
- modify: crates/norn-tui/src/app/mod.rs
- modify: crates/norn-tui/src/app/state.rs
- modify: crates/norn-tui/src/app/render.rs
- modify: crates/norn-tui/src/app/event_loop.rs
- modify: crates/norn-tui/src/app/turn/mid.rs
- modify: crates/norn-tui/src/app/helpers.rs
- modify: crates/norn-tui/src/app/autocomplete.rs
- modify: crates/norn-tui/src/render/mod.rs
- modify: crates/norn-tui/src/render/fixed_panel.rs
- modify: crates/norn-tui/src/render/content.rs
- modify: crates/norn-tui/src/render/syntax.rs
- modify: crates/norn-tui/src/render/markdown.rs
- modify: crates/norn-tui/src/render/markdown/emitter.rs
- modify: crates/norn-tui/src/render/markdown/dim.rs
- modify: crates/norn-tui/src/render/markdown/dim/list.rs
- modify: crates/norn-tui/src/render/markdown/scan.rs
- modify: crates/norn-tui/src/render/markdown/scan/list.rs
- modify: crates/norn-tui/src/render/thinking.rs
- modify: crates/norn-tui/src/render/text.rs
- modify: crates/norn-tui/src/render/style.rs
- modify: crates/norn-tui/src/render/streaming_indicator.rs
- modify: crates/norn-tui/src/events/schema_render.rs
- modify: crates/norn-tui/src/agents/status_line.rs
- modify: crates/norn-tui/src/agents/activity_log.rs
- modify: crates/norn-tui/src/agents/tabs.rs
- modify: crates/norn-tui/src/terminal/setup.rs
- modify: crates/norn-tui/src/terminal/caps.rs
- modify: crates/norn-tui/Cargo.toml
- modify: Cargo.lock
- modify: crates/norn-tui/src/agents/mod.rs
- modify: crates/norn-tui/src/tools/rich/edit.rs
- modify: crates/norn-tui/src/render/layout.rs
- modify: crates/norn-tui/src/render/layout_tests.rs
- delete: crates/norn-tui/src/render/scroll_region.rs

**Checklist:**
- C4 — Own frame, layout, caret and styled content

**Stories:**
- S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.

### R5: Compact known/unknown calls and honest per-call Changes

WHEN a tool is shown, THE SYSTEM SHALL display compact description/status and allow historical expansion or a read-only exact call change view without touching provider context or workspace files.

**Acceptance:**
- Every known and unknown tool starts compact: the collapsed row prioritizes the original tool-use description and lifecycle, with concise duration when recorded. Missing description is explicit. Exact call ID, commitment, duration availability and description parse errors remain visible in expanded detail or the selected Changes view; they do not add repeated technical bookkeeping to every collapsed row. Individual historic expansion obeys D-F3 override rules.
- Real edit/write/patch output shapes prove committed-with-diagnostic-error distinct from blocked/uncommitted; no action-log-success filter.
- Opening an old link reads its recorded argument/result/spool references only, even after disk changes or deletion. Missing before-content cannot produce an empty-baseline diff.
- Bash/MCP/unknown writer coverage remains explicitly unknown; no invented authorship or session-wide diff. Changes exposes no save/mutation handler.
- Design provenance: D3, change_evidence_rules, I5-I6, WS-03, WS-05, WS-06 in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.
- Parallel authoring exception: the four pure summary/changes implementation and test files may be authored while R4 integrates. No module wiring or shared-state edits in that slice; R4 remains the prerequisite for R5 integration, validation and completion.
- app/render/changes.rs owns selected-call parsing/cache and direct read-only Changes preparation, keeping app/render.rs below500 production lines. It consumes only complete approved exact BodyRefs already supplied by the caller; no filesystem reads, source relabelling or session-wide diff inference.
- R3/R5 completion requires the NRT-002 exact producer publication-binding repair and its required proof. Existing implementation/integration authoring may continue under exclusive coordinated ownership; duplication, guessed association or an unverified repair cannot be reported complete. NRT-001 and NRT-002 form one final checkpoint; completion gates do not add circular source-authoring prerequisites.
- For one exact admitted execution, normal End and TurnCompleted facts share one compact completed-execution presentation row. Selection or expansion exposes every original completion fact, source, model, usage, timing and availability field. Errors, cancellation and coverage gaps remain explicit. Never drop metadata, invent success or associate records by text, time or proximity. Implement presentation only through existing TUI file walls.
- Tom's no-allow-anywhere instruction overrides the inherited CLAUDE.md test exception. Before landing, remove inherited allow attributes and resolve the underlying test unwrap/expect uses in the eleven already-modified TUI files named by this partition. Preserve every assertion and propagate failures through Result; no skips, weaker assertions, renamed unused values or replacement lint bypasses. This is test-only cleanup of these current modified files, not unrelated unchanged-module or public-history audit work. Exclusive test-cleanup partition: review_child_context owns test-only cleanup in crates/norn-tui/src/agents/tabs.rs, crates/norn-tui/src/app/autocomplete.rs, crates/norn-tui/src/app/helpers.rs, crates/norn-tui/src/events/schema_render.rs, crates/norn-tui/src/render/fixed_panel.rs, crates/norn-tui/src/render/markdown.rs, crates/norn-tui/src/render/text.rs. norn_tui retains crates/norn-tui/src/app/dispatch.rs, crates/norn-tui/src/app/event_loop.rs, crates/norn-tui/src/app/slash.rs, crates/norn-tui/src/app/state.rs. The active source owner must explicitly acknowledge handoff before parallel cleanup starts; no concurrent writes to any shared file. Production behavior remains unchanged, and all current brief acceptance and final verification requirements remain.

**Files:**
- create: crates/norn-tui/src/tools/summary.rs
- create: crates/norn-tui/src/tools/summary_tests.rs
- create: crates/norn-tui/src/app/changes.rs
- create: crates/norn-tui/src/app/changes_tests.rs
- create: crates/norn-tui/src/render/changes.rs
- create: crates/norn-tui/src/app/render/changes.rs
- modify: crates/norn-tui/src/app/mod.rs
- modify: crates/norn-tui/src/app/state.rs
- modify: crates/norn-tui/src/app/tool_calls.rs
- modify: crates/norn-tui/src/render/mod.rs
- modify: crates/norn-tui/src/render/content.rs
- modify: crates/norn-tui/src/tools/mod.rs
- modify: crates/norn-tui/src/tools/renderer.rs
- modify: crates/norn-tui/src/tools/helpers.rs
- modify: crates/norn-tui/src/tools/compact.rs
- modify: crates/norn-tui/src/tools/minimal.rs
- modify: crates/norn-tui/src/tools/status.rs
- modify: crates/norn-tui/src/tools/verbosity.rs
- modify: crates/norn-tui/src/tools/rich/bash.rs
- modify: crates/norn-tui/src/tools/rich/edit.rs
- modify: crates/norn-tui/src/tools/rich/patch.rs
- modify: crates/norn-tui/src/tools/rich/read.rs
- modify: crates/norn-tui/src/tools/rich/search.rs

**Checklist:**
- C5 — Compact known/unknown calls and honest per-call Changes

**Stories:**
- S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.

### R6: Reading, selection/copy, search/export and existing controls

WHEN a person navigates retained content, THE SYSTEM SHALL provide keyboard and mouse actions with explicit focus/capability/scope, while preserving current composer and runtime-control behavior.

**Acceptance:**
- Selection binds original logical text/body revision across Unicode, wrapping, resize and paging. Copy strips UI/control bytes and never converts soft wraps into hard newlines; unavailable or unacknowledged clipboard is not reported as confirmed success.
- Scoped search/export cover older history through explicit demand with partial/unavailable states; disk export targets an explicit operator path and reports errors/overwrite decisions without hidden persistence.
- Built-in slash list derives from existing shared registry, plus selected skills/@file autocomplete. Popup selection precedes send/view keys; keyboard-only focus/expand/scroll/follow/search/copy/export/resize is discoverable.
- Multiline paste never submits. Preserve Unicode editing/history, Enter/newline capability behavior, Escape popup/draft handling, Ctrl+C active cancellation/idle exit, Ctrl+T steer/queue, Ctrl+E reasoning/secondary fields, model/effort/tier and NCS settings validation, usage/status and MCP command-exit guards.
- Existing child activity and result delivery remain visible. View selection does not grant new worker control or change message targets.
- Register /view once in the shared built-in catalogue as TUI-only. Preserve exhaustive CLI enum handling without installing a CLI action or sending view commands to the model. Slash completion/help use this same entry.
- The /view palette lists and invokes every view action and explicit settings: focus, pane visibility, split, browse, expand/reset overrides, follow/pin, selection, copy/export, search scope/load-more, history/body demand and clipboard capability. Mid-turn /view is intercepted locally before steer/queue admission; other input behavior is unchanged.
- F6/Shift+F6 cycles visible regions; F2 toggles upper pane; PgUp/PgDn browse focus; non-composer Enter expands; F3 search, F4 copy, F5 export; arrows resize a focused divider. Existing composer/popup/cancellation actions have precedence. Unavailable function keys have the /view equivalent.
- Design provenance: viewport_invariants, F2-F5, WS-11, WS-16, producer-coverage.json in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.
- Parallel authoring exception: selection.rs and selection_tests.rs may be authored as a pure source/revision-bound original-byte selection module while R4/R5 integrate. No clipboard, filesystem, module wiring or runtime mutations belong in that slice. R5 remains prerequisite for R6 integration, validation and completion.
- Parallel authoring exception: terminal/clipboard.rs may be implemented independently as a pure explicit-capability copy-preparation module with inline tests, using existing termina OSC 52 formatting. No terminal probing, writes, environment reads, clipboard reads, new dependencies or shared module wiring in this slice. Unspecified/disabled capability emits no sequence; integration alone owns writes and may report sent only after write/flush succeeds, never acknowledged clipboard success. R5 remains integration/completion prerequisite.
- Hit testing and mouse dispatch validate the current frame source, body revision and rendered source-map identity. A logical placeholder anchor may preserve a requested item/offset while content is unavailable, but it is not the identity of a painted source map and cannot authorize selection, copy or an unrelated hit. Keep those identities distinct; stale/missing mappings are explicit and never rebound through a placeholder fallback.
- Parallel authoring exception: app/search.rs and app/search_tests.rs may be authored independently as a pure literal-query search module over source-bound original text prefixes, with grapheme-safe match/selection boundaries. Every result retains exact source/body revision and original-byte coordinates and declares loaded-transcript, selected-body or explicitly requested older-history scope, including partial/unavailable coverage. An unsearched suffix or older range is never reported as no match. No I/O, provider work, module wiring or runtime mutation belongs in this slice. R5 remains prerequisite for R6 integration, validation and completion.
- Parallel authoring exception: app/export.rs and app/export_tests.rs may be authored independently for exporting explicitly scoped, source/revision-bound original text to an explicit operator path. Default writes create a new file; replacement requires an explicit overwrite choice. Preserve original hard newlines and report missing/partial source coverage and path-specific errors; no hidden transcript persistence. Filesystem work must run off the terminal event loop when integrated. This authoring slice does not wire modules, dispatch runtime work or mutate session/provider state. R5 remains prerequisite for R6 integration, validation and completion.
- Split reading-action orchestration into app/view_actions/reading.rs and reading_tests.rs: typed search state, navigation to an exact source/body-revision hit, and supervised explicit-path export I/O workers. Existing commands.rs owns command grammar. Preserve current-frame source/map identity independently of logical placeholder anchors, source-scoped partial/unavailable search/export status, create-new by default and explicit overwrite choice. Export filesystem work stays off the terminal event loop; worker completion/errors remain supervised and source-bound. This adds no provider, persistence or session-control capability. R5 remains prerequisite for R6 integration, validation and completion.
- Tom's no-allow-anywhere instruction overrides the inherited CLAUDE.md test exception. Before landing, remove inherited allow attributes and resolve the underlying test unwrap/expect uses in the eleven already-modified TUI files named by this partition. Preserve every assertion and propagate failures through Result; no skips, weaker assertions, renamed unused values or replacement lint bypasses. This is test-only cleanup of these current modified files, not unrelated unchanged-module or public-history audit work. Exclusive test-cleanup partition: review_child_context owns test-only cleanup in crates/norn-tui/src/agents/tabs.rs, crates/norn-tui/src/app/autocomplete.rs, crates/norn-tui/src/app/helpers.rs, crates/norn-tui/src/events/schema_render.rs, crates/norn-tui/src/render/fixed_panel.rs, crates/norn-tui/src/render/markdown.rs, crates/norn-tui/src/render/text.rs. norn_tui retains crates/norn-tui/src/app/dispatch.rs, crates/norn-tui/src/app/event_loop.rs, crates/norn-tui/src/app/slash.rs, crates/norn-tui/src/app/state.rs. The active source owner must explicitly acknowledge handoff before parallel cleanup starts; no concurrent writes to any shared file. Production behavior remains unchanged, and all current brief acceptance and final verification requirements remain.
- Core test-only cleanup within current modified source walls: review_child_context exclusively owns loop/commands.rs, session/branch.rs and session/spool.rs under crates/norn/src. norn_models retains loop/classify.rs and provider/agent_event.rs. Remove the five inherited allow attributes and resolve their underlying test unwrap/expect uses, preserving every semantic assertion and production bytes for this cleanup. Propagate Result failures; no skips, weaker assertions, wildcard bypasses or unrelated unchanged-module changes. Common-owner Clippy style repairs are a separately identified existing task, not permission to change production through this test-cleanup partition. The 52-file pre-thaw core AST snapshot remains historical evidence. Source ownership must be acknowledged before parallel cleanup; no concurrent writes to a shared file. The whole-workspace Clippy01 run has ended and root explicitly released the document freeze. These are existing paths only and all NRT acceptance and final verification requirements remain.
- The existing modified-core test cleanup includes inherited panic/panic!, expect_err and unwrap_err as well as unwrap and expect. Replace these with contextful fallible Result diagnostics while preserving every original semantic assertion and production bytes for the cleanup. No new skips, weaker assertions, or replacement bypasses.
- Root records the TUI owner's explicit handoff of test-only crates/norn-cli/src/commands/slash/registry.rs to review_child_context. This existing NRT-001 R6 path has one inherited allow attribute and thirteen test unwrap/expect/panic operations. Remove those structurally with fallible Result helpers/tests, preserving all semantic assertions, test attributes, expected failures and resource lifetime guards. The production prefix must remain byte-identical to the final core/CLI audit. No suppression, skip, weaker oracle or unrelated module change.

**Files:**
- create: crates/norn-tui/src/app/view_actions.rs
- create: crates/norn-tui/src/app/selection.rs
- create: crates/norn-tui/src/app/selection_tests.rs
- create: crates/norn-tui/src/app/search.rs
- create: crates/norn-tui/src/app/search_tests.rs
- create: crates/norn-tui/src/app/export.rs
- create: crates/norn-tui/src/app/export_tests.rs
- create: crates/norn-tui/src/terminal/clipboard.rs
- create: crates/norn-tui/src/app/view_actions/commands.rs
- create: crates/norn-tui/src/app/view_actions/keys.rs
- create: crates/norn-tui/src/app/view_actions/mouse.rs
- create: crates/norn-tui/src/app/render/hit.rs
- create: crates/norn-tui/src/app/view_actions/reading.rs
- create: crates/norn-tui/src/app/view_actions/reading_tests.rs
- modify: crates/norn-tui/src/app/mod.rs
- modify: crates/norn-tui/src/app/event_loop.rs
- modify: crates/norn-tui/src/app/turn/mid.rs
- modify: crates/norn-tui/src/app/edit.rs
- modify: crates/norn-tui/src/app/autocomplete.rs
- modify: crates/norn-tui/src/app/slash.rs
- modify: crates/norn-tui/src/app/slash_catalog.rs
- modify: crates/norn-tui/src/input/keybindings.rs
- modify: crates/norn-tui/src/terminal/mod.rs
- modify: crates/norn-tui/src/terminal/caps.rs
- modify: crates/norn-tui/src/terminal/setup.rs
- modify: crates/norn/src/loop/commands.rs
- modify: crates/norn-cli/src/commands/slash/registry.rs
- modify: crates/norn-tui/src/app/view_config.rs
- modify: crates/norn-tui/src/app/view_config_tests.rs

**Checklist:**
- C6 — Reading, selection/copy, search/export and existing controls

**Stories:**
- S3 (Session participant, human operator or observing model) — As a keyboard or mouse reader I can find, select, copy and export content and retain all current controls, with real terminal proof.

### R7: Real PTY checkpoint and exact-commit evidence

WHEN the integrated checkpoint is verified, THE SYSTEM SHALL exercise actual run_app in a real PTY with deterministic provider/tool/store fixtures and verify semantic invariants alongside terminal behavior before venue release checks.

**Acceptance:**
- Execute PTY-01..PTY-09 in acceptance.json; use real PTY dimensions/events, barrier-controlled provider fixtures and real tool-result semantics. Missing prerequisites fail clearly; no silent skips, no provider credentials required.
- Existing model/budget/prompt-history, channel wake/hold/next-turn and child/cancel tests retain their semantic assertions; replace old escape-sequence shape assertions with new owned-frame/restoration assertions.
- Supported real terminal and multiplexer selection/copy/restoration require recorded manual evidence; PTY OSC52 bytes alone cannot prove actual clipboard acceptance. Capture old artifact if reproducible; otherwise label initial diagnosis source-supported, not runtime-reproduced.
- Run strict fmt/all-target Clippy and declared battery legs on final exact commit at205, with source/gates/guard/venue witnesses and raw result references. Required rigorous review and source-transfer authorization are separate from a green battery. No local pass/fixture timing is a landing receipt or performance claim.
- Design provenance: acceptance.json, D5, WS-01, WS-02, WS-03, WS-11, WS-16 in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.
- Parallel fixture-authoring exception: after the current source freeze ends, channels_fixture alone may update the existing canonical pty_smoke.rs, model_selection.rs, mcp_channels.rs and support/mcp_channels_tui.rs. The already-declared retained_workspace.rs and support/retained_workspace.rs may be authored by a separate explicitly assigned owner. Preserve every semantic assertion and PTY acceptance. This permits authoring only: R7 validation/completion still requires R6 and the NRT-002 publication-binding repair. No source collision, early acceptance or extra path is authorized.
- Use tests/support/retained_screen.rs as one shared existing-vte terminal observer for the canonical PTY suites, owned only by channels_fixture. Preserve real visible geometry, alternate-screen/resize/mode and exact-occurrence assertions plus real termios restoration; do not replace semantic assertions with weaker substring checks. No new dependency, clipboard acknowledgement, provider certification or broader terminal-matrix claim is introduced.
- The already-registered R7 new_retained_workspace parallel-authoring partition is assigned to review_tui, exclusively for tests/retained_workspace.rs and tests/support/retained_workspace.rs. Author persistent actual-App PTY-04 reading-control/resize fixtures using the shared retained_screen oracle API and the existing 33-check external R6 evidence as a source of scenarios, not a substitute for executing these fixtures. channels_fixture remains the exclusive canonical-fixture/shared-oracle owner. No source paths or acceptance scope are added, and R6/NRT-002 prerequisites still gate R7 validation/completion.
- Within the existing canonical pty_smoke.rs wall, channels_fixture owns the child-result reporting repair: do not discard tx.send(result).await. The supervised fixture must retain and surface send failure, including the undelivered child outcome, and inspect the spawned task outcome; merely joining an ignored Result is insufficient. Preserve the existing actual-App assertions, deadline/termination behavior and the original application failure as evidence. This is fixture error supervision, not permission to weaken failed scenarios or alter production code.
- Root owns the minimal checked-preview version step before the next full build: change only workspace.package.version in Cargo.toml from 0.1.0 to 0.1.0-preview.1, and only the four local workspace package versions norn, norn-cli, norn-macros and norn-tui in Cargo.lock. No dependency resolution or dependency version/source/checksum change. Create docs/release-notes/0.1.0-preview.1.md naming actual preview features and known limitations; do not rewrite historical UNRELEASED.md or add an installer/CHANGELOG. Existing Clap Cargo version and protocol version fields derive from the Cargo package version; the inspected CLI tests contain no hard-coded 0.1.0 version assertion needing a new path. This root-owned metadata authoring may precede R7 final verification, whose R6/NRT-002 prerequisites remain unchanged.
- User explicitly authorizes ongoing checked local preview installations. Increment the preview prerelease number for each subsequent successful checked update; first stable 0.1.0 still requires proper release checks. Before replacement, verify candidate source/artifact hash, version/help behavior, and a recoverable hash-verified backup. After replacement, verify installed hash/version and record the receipt/rollback path, then invoke macOS /usr/bin/say with the exact text Norn updated. If speech fails, retain the truthful installed state and report the announcement failure. No tag, public release, main merge, background updater, extra service or wider installer implementation is implied. This local preview procedure does not replace exact-candidate release/landing verification.

**Files:**
- create: crates/norn-tui/tests/retained_workspace.rs
- create: crates/norn-tui/tests/support/retained_workspace.rs
- create: crates/norn-tui/tests/support/retained_screen.rs
- create: docs/release-notes/0.1.0-preview.1.md
- modify: crates/norn-tui/tests/pty_smoke.rs
- modify: crates/norn-tui/tests/model_selection.rs
- modify: crates/norn-tui/tests/mcp_channels.rs
- modify: crates/norn-tui/tests/support/mcp_channels_tui.rs
- modify: Cargo.toml
- modify: Cargo.lock

**Checklist:**
- C7 — Real PTY checkpoint and exact-commit evidence

**Stories:**
- S3 (Session participant, human operator or observing model) — As a keyboard or mouse reader I can find, select, copy and export content and retain all current controls, with real terminal proof.

### R8: R4.layout — independent pure geometry foundation

WHEN given terminal geometry, requested composer rows and explicit pane/split policy, THE SYSTEM SHALL calculate rectangles and a typed no-paint/resize-required/single/split result without session semantics, terminal I/O or mutable application state.

**Acceptance:**
- Expose a pure LayoutRequest/LayoutPolicy to Layout result seam. Inputs include cols/rows, requested composer rows, Changes visibility, preferred split and active upper pane/focus. Results provide bounded composer/content/divider rectangles and effective mode; saved preferences are inputs, never rewritten when clamped. No imports from norn::session_view or app state.
- Declare default equal upper-pane split, minimum content width40 each plus one divider, composer maximum12 rows and at most half terminal rows. Width81 is the derived split threshold. Explicit validated policy overrides win; no second copy of these defaults elsewhere.
- Allocate odd split surplus deterministically to conversation, clamp actual split to both minima, and retain the original preference for widening. Changes closed gives conversation all upper width; narrow Changes-open uses the selected single upper pane.
- Zero width or height returns no paint. Geometry unable to retain at least one composer row and one conversation/content row returns resize-required while preserving caller state. Otherwise requested composer height is clamped by declared policy; integration adds popup only if it fits after essential content.
- Test widths80/81/82, height0/1/2, empty/oversized requested composer height, extreme u16 geometry, custom validated policy, open/closed/narrow pane selection, repeated shrink/widen with unchanged preferences and rectangle containment/non-overlap. Test numbers are geometry cases, not a performance SLA.
- Root alone edits these two files. No render/mod.rs wiring in this row; R4 performs it after R3 and completed R8. Files have documented public APIs and mod.rs policy remains intact.
- Design provenance: D-F2, declared_defaults, viewport_invariants in ../design.json; producer obligations ../producer-coverage.json; PTY oracles ../acceptance.json.

**Files:**
- create: crates/norn-tui/src/render/layout.rs
- create: crates/norn-tui/src/render/layout_tests.rs

**Checklist:**
- C8 — R4.layout — independent pure geometry foundation

**Stories:**
- S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.

### R9: R4.text — independent grapheme-safe styled row geometry

WHEN supplied validated plain displayed text, ordered styles and explicit width/tab geometry, THE SYSTEM SHALL wrap, clip and map complete graphemes without terminal I/O or session semantics.

**Acceptance:**
- Use one plain displayed string with ordered typed style byte ranges. Segment extended grapheme clusters across style-span boundaries, then produce cell-bounded styled rows with displayed-byte ranges and hit-test boundaries. Style is data, never embedded ANSI. No norn/session types, body reads, terminal I/O, application state or provider work belongs in this module. The integration adapter separately maps original approved body bytes through visible escaping into these displayed-byte positions.
- Validate style ranges at construction: in-bounds, ordered/nonoverlapping, valid UTF-8 boundaries; malformed ranges and disallowed terminal/bidi controls produce typed errors with their offsets. Keep hard newlines and explicitly parameterized tab stops. Zero target width produces an explicit nonpaint result without losing caller text or looping; zero-column graphemes preserve source identity and make progress.
- Greedy wrapping preserves hard lines, spaces and whole extended grapheme clusters. Clip to a bounded cell interval without emitting half of a wide cluster or silently rewriting the saved logical range. A cluster wider than available columns is explicitly unpaintable at that geometry; resizing can reveal it unchanged.
- Tests include CJK width, ZWJ emoji, combining marks, variation selectors and style boundaries inside a cluster; tabs at multiple columns with explicit tab-stop policy; empty strings/hard newlines/trailing spaces; invalid ranges/controls; zero/tiny width; greedy wrap; clipping at both halves of wide cells; byte-range-to-cell and cell-hit-to-byte mapping. Assertions distinguish displayed-byte coordinates from original body bytes.
- Root alone owns these exact two files. This row depends only on R0 and may run alongside R1/R2/R3. It does not wire render/mod.rs or modify Cargo files; R4 integrates it after R3, R8 and R9 and owns the already-approved unicode-segmentation manifest/lock change.
- A small external diagnostic harness may use the existing resolved unicode-segmentation/unicode-width packages to exercise this pure seam before workspace wiring. Label that evidence as bounded local diagnostics, never a workspace or venue receipt.
- Design provenance: D-R4-TEXT, D-R4-GRAPHEMES and viewport_invariants in ../design.json; R4 owns the final full-frame and real PTY acceptance.

**Files:**
- create: crates/norn-tui/src/render/retained_text.rs
- create: crates/norn-tui/src/render/retained_text_tests.rs

**Checklist:**
- C4 — Own frame, layout, caret and styled content

**Stories:**
- S2 (Session participant, human operator or observing model) — As a participant I can keep reading a compact conversation and exact call changes while the terminal resizes and work continues.

## Boundaries

- No provider/tool execution engine changes, session-host/Ready transport, active-input policy change, Liminal dependency, Iridium embedding, filesystem watcher, session baseline capture or mutation-owner work in this checkpoint. Those remain wanted programme work, not permanent feature cuts.
- No added dependencies or arbitrary configuration values. R4 adapts existing presentation APIs and the named Cargo package description only; dependency changes are outside the wall.
- No new durable transcript store, raw terminal recording, ANSI emulator or sidecar agent. Use existing EventStore plus one semantic view.
- Root owns programme/tracking updates and NCS integration outside these product walls.
- No silent skipped prerequisites, lint bypasses, or reduction of existing semantic assertions. Source changes need the existing rigorous review and exact-commit venue receipt.

## Verification

- Run row acceptance and every PTY scenario in ../acceptance.json; semantic operation counts and screen behavior are separate oracles.
- Run existing estate renderer and coverage checker after any authoritative brief change; validate all local references and exact row-wall ownership.
- Run strict fmt/all-target Clippy, then declared exact-commit NRT battery on205 with source/gates/guard/venue witnesses; no skipped prerequisites or borrowed NCS receipt.
- Obtain the required rigorous review with the applicable source-transfer authorization; retain real supported-terminal and multiplexer evidence for clipboard/selection/restoration.
- NCS exact-green and integration are landing prerequisites. If that baseline changes, reconcile/rebase and repeat NRT final verification.
