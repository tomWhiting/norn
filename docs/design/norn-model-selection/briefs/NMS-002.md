---
type: brief
id: NMS-002
cluster: norn-model-selection
title: Owner-selected Astra startup defaults
---

# NMS-002: Owner-selected Astra startup defaults

> **Cluster:** norn-model-selection
> **Depends on:** NMS-001
> **Checklist:**
> - C1 — The selected Codex model uses reviewed facts and the advertised typed effort vocabulary.
> - C2 — Model capability and budget resolution uses the actual backend and explicit override provenance.
> - C3 — Live switches publish validated model and both budgets together or change nothing.
> **Stories:**
> - S1 (Norn user, operator) — I can select Astra with its actual Codex capabilities and see honest support boundaries.
> - S2 (Norn user, operator) — My selected backend enforces its own model limits before any provider call.
> - S3 (Norn user, operator) — Switching models updates protection correctly and a refused switch leaves my session intact.

## Purpose

Apply Tom’s 5 September 2026 Melbourne request: Astra, high reasoning, and a larger configurable default context; coordinator selects the proposed 372000 option. NMS-001 owns the original catalogue and transition work; NMS-002 owns only the later defaults amendment to shared C1-C3/S1-S3.

## Task

R1 changes only the named defaults, wiring, matching assertions and documentation. Leave source ready for Tom to rebuild; do not reinstall or claim a new landing receipt.

## Requirements

### R1: Separate Norn operating defaults from provider metadata

Default CLI model is gpt-6-astra. With no explicit effort, CLI requests high on the Codex route when that model declares high. Astra on Codex derives a 372000 context window when no explicit window exists. Explicit settings/profile/CLI values win. Model changes retain derived-versus-explicit context provenance. Other routes never borrow Codex metadata. Provider snapshot values remain unchanged.

**Acceptance:**
- Default profile resolves Astra; CLI assembly requests High with derived372000.
- Explicit context444000 and Low effort override defaults.
- Switching Sol→Spark→Astra derives272000→128000→372000; explicit windows stay explicit.
- Snapshot still records Astra272000/872000 and medium; public-route missing metadata still refuses derived windows.
- Existing model/CLI/TUI transition assertions updated; no lint suppressions added.

**Files:**
- create: crates/norn/src/model_selection/defaults.rs
- modify: assets/models.json
- modify: crates/norn/src/model_catalog.rs
- modify: crates/norn/src/model_selection.rs
- modify: crates/norn/src/model_selection/tests.rs
- modify: crates/norn/tests/codex_catalogue_snapshot.rs
- modify: crates/norn-cli/src/config/profile_loader.rs
- modify: crates/norn-cli/src/runtime/resolve.rs
- modify: crates/norn-cli/tests/model_selection.rs
- modify: crates/norn-tui/src/app/model_selection.rs
- modify: crates/norn-tui/tests/model_selection.rs
- modify: docs/release-notes/UNRELEASED.md
- modify: docs/design/norn-model-selection/design.json
- modify: docs/design/norn-model-selection/DESIGN.md

**Checklist:**
- C1 — The selected Codex model uses reviewed facts and the advertised typed effort vocabulary.
- C2 — Model capability and budget resolution uses the actual backend and explicit override provenance.
- C3 — Live switches publish validated model and both budgets together or change nothing.

**Stories:**
- S1 (Norn user, operator) — I can select Astra with its actual Codex capabilities and see honest support boundaries.
- S2 (Norn user, operator) — My selected backend enforces its own model limits before any provider call.
- S3 (Norn user, operator) — Switching models updates protection correctly and a refused switch leaves my session intact.

## Boundaries

- This owner decision supersedes the old keep-Sol instruction in NMS-001 R1.
- No mutation of supplied provider metadata, credentials or installed binary.
- Working-tree checks do not replace the exact-commit205 battery or pending Fable review.

## Verification

- cargo fmt --all -- --check
- cargo clippy --workspace --all-targets -- -D warnings
- Focused catalogue, default CLI assembly and existing model/TUI transition tests
