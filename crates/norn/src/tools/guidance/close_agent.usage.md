Use to gracefully shut down a running agent — for example, a long-lived worker that should stop after its current task. The close is cooperative: the agent is not killed immediately but transitions to Completing status, which the agent loop checks between iterations. Provide a reason string to explain why the agent is being closed — this is recorded in the registry for observability.