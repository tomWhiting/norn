Always read a file before editing or writing to it. Reads are bounded by line and character budgets; when output is truncated, use `next_offset`, narrower `offset`/`limit` windows, or search for specific terms instead of asking for the whole file. Binary and image files are detected automatically and reported without dumping raw bytes. File paths must be absolute.
