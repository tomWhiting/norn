Use for creating new files or completely replacing file contents. Prefer edit for surgical changes to existing files — write replaces the entire file. An existing file must be read first unless the orchestrator sets AllowOverwrite. Parent directories are created automatically. Syntax errors and length-limit violations are reported but do not prevent the write (report semantics). File paths must be absolute.