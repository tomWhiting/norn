Fetch a URL and extract answers via an internal extraction agent. The page is fetched, converted to clean markdown, saved to disk, and processed by an extraction model. Provide `questions` (array of strings) to get specific answers with line number citations; omit for a general summary. Returns structured answer objects with line references to the saved document. Use `detail` to control answer depth (brief/normal/detailed).
