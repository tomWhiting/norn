//! Retained-evidence redaction integration fixtures.

mod redaction {
    mod authoring;
    mod authority;
    mod formats;
    mod gate_authoring;
    mod inventory;
    mod protocol;
    mod references;
    mod support;
}
