//! Immutable-origin and legacy-governance tests.

mod baseline {
    mod evaluation;
    mod governance;
    mod governance_authoring;
    mod items;
    mod origin;
    mod production;
    mod support;
}
